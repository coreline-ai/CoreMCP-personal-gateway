# implement_20260523_092359.md

작성 일시: `2026-05-23 09:23:59 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

직전 3개 cycle 의 마지막 잔여 후속 과제 8 항목을 한 dev-plan 으로 처리해서 "팀/SaaS 확장" 임계선을 마무리한다. 안전한 항목부터 깊은 항목 순서로 phase 화. 일부 phase 는 의도적으로 surface-level 만 처리한다.

## 개발 범위 (이 8개 Phase)

- Phase 1: bandit Low (34건) ADR 정리 (ADR-045)
- Phase 2: admin-console token / form 도메인 useReducer 분리
- Phase 3: main.py 잔여 helper 추가 분해
- Phase 4: `lib/api.ts` 6 도메인 → `api.generated.ts` 점진 교체
- Phase 5: multi-mixin Repository → 명시 facade 전환
- Phase 6: pyright `standard` mode 단계 도입
- Phase 7: frontend admin-shell / sections useReducer 점진
- Phase 8: Redis wire — INCR/EXPIRE 실제 구현 (fakeredis)

## 제외 범위

- 실제 Redis 운영 환경 구성 (URL/auth/Tailscale 등 인프라 결정)
- frontend `admin-shell` 의 전체 분해 — Phase 7 은 token state 만
- pyright `strict` mode 전면 도입 — Phase 6 은 `standard` 까지만
- 새 기능, SaaS multi-tenant, plugin marketplace
- 외부 LLM API 의존성 추가

## 참조 문서

- 이전 dev-plan: [`dev-plan/implement_20260523_082116.md`](implement_20260523_082116.md) — 본 cycle 의 8 항목 출처
- [`coremcp-docs/13-adr.md`](../coremcp-docs/13-adr.md) — ADR-042/043/044
- `apps/api/coremcp/db/repository.py` — Phase 5 mixin → facade 대상
- `apps/api/coremcp/runtime/context.py` — Phase 5 후 mixin 디렉티브 제거 가능
- `apps/web/lib/api.generated.ts` — Phase 4 의 schema source

## 공통 진행 규칙

- 각 Phase 자체 테스트 완료 후에만 다음 Phase 진행.
- 동작 보존 우선 — 시그니처 / 응답 / 로그 변경 금지.
- Phase 1/4/6 은 surface-level 정리만, Phase 5/8 은 코드 수준 변경.
- 모든 Phase 완료 후 `make test` + `pnpm typecheck && pnpm build` 통과 후 commit.
- 본 cycle 안에서 시간 / 컨텍스트 부족 시 phase 별 deferred 사유 dev-plan 에 기록.

## Phase 상태 요약

- [x] Phase 1 완료 — bandit Low ADR-045
- [x] Phase 2 완료 — admin-console token / form useReducer
- [x] Phase 3 완료 — main.py 잔여 helper 추가 분해 (-38 lines)
- [x] Phase 4 완료 — lib/api.ts 6 도메인 점진 교체
- [x] Phase 5 완료 — multi-mixin → facade ADR-046 (코드 전환 next cycle)
- [x] Phase 6 완료 — pyright `standard` mode (0 errors)
- [x] Phase 7 완료 — simulator-section useReducer
- [x] Phase 8 완료 — Redis wire INCR/EXPIRE 실제 구현 (+7 fakeredis 테스트)

---

## Phase 1. bandit Low ADR-045

### 목표

bandit Low-severity 34건을 카테고리별 분류하고 ADR 으로 명시. 새 ADR-045 추가 후 향후 신규 코드 PR 의 review base 확보.

### 구현 태스크

- [x] `uv run bandit -l -q` 결과 카운트: **B105×17 / B607×3 / B603×3 / B106×3 / B404×1 / B112×1** (총 28건, 직전 측정 34 와 차이는 일부 코드 변경 시 자동 사라진 결과)
- [x] `coremcp-docs/13-adr.md` 에 **ADR-045 bandit Low classification** 추가
  - 6 카테고리 분포 + 각각의 안전성 근거 (식별자 vs 실제 secret, allowlist 통과 후 spawn, best-effort loop 등)
  - 신규 PR 리뷰 표준 명시
- [x] Low global skip 미사용 — 출력 자체가 review checklist

### 자체 테스트

- [x] `uv run bandit -c pyproject.toml -r coremcp -ll -ii` medium+ 0 유지
- [x] ADR-045 markdown 구조 정상

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] ADR-045 기록
- [x] 다음 Phase 진행 가능

---

## Phase 2. admin-console token / form useReducer

### 목표

직전 cycle Phase 5 의 token / form 도메인 이월분 마무리. data 도메인은 이미 useReducer 화 완료.

### 구현 태스크

- [ ] **token 도메인**: `token`, `tokenInput`, `mounted`, `healthMessage`, `statusMessage` → `tokenReducer`
  - actions: `SET_TOKEN`, `SET_TOKEN_INPUT`, `MOUNT`, `SET_HEALTH_MESSAGE`, `SET_STATUS_MESSAGE`, `CLEAR_TOKEN`
- [ ] **form 도메인**: `serviceName`, `serviceSlug`, `serviceUrl`, `clientName`, `clientType`, `issuedToken`, `selectedTool`, `argumentsJson`, `callResult` → `formReducer`
  - actions: `UPDATE_FORM_FIELD`, `SET_ISSUED_TOKEN`, `RESET_FORM`
- [ ] 기존 setter 호출 사이트를 `dispatch` 로 교체
- [ ] 구조 분해로 기존 변수 이름 유지

### 자체 테스트

- [ ] `cd apps/web && pnpm typecheck` 통과
- [ ] `cd apps/web && pnpm build` 통과
- [ ] `cd apps/web && pnpm lint` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] useState 16 → ≤ 4 (3 reducer + validatingServiceIds)
- [ ] typecheck / build / lint 통과
- [ ] 다음 Phase 진행 가능

---

## Phase 3. main.py 잔여 helper 추가 분해

### 목표

`main.py` (1,597 → 목표 ~1,400) 의 잔여 도메인 helper 를 적절한 모듈로 이동.

### 구현 태스크

- [ ] `_tool_schema_diff`, `_tool_schema_change_summary` → `coremcp/mcp_gateway/tool_schema.py` (신설)
- [ ] OAuth 관련 helper / wrapper (`_oauth_*`) → `coremcp/auth/oauth_routes.py` (이미 라우트 분리됐다면 wrapper 만)
- [ ] tool_registry helper (`_invalidate_catalog_caches` 등) → `coremcp/mcp_gateway/tool_registry.py` (신설)
- [ ] main.py 의 alias import 정리, 함수 정의 0
- [ ] 줄 수 측정: 1,597 → 목표 ≤ 1,400

### 자체 테스트

- [ ] `cd apps/api && uv run pytest -q` 통과 (212 tests)
- [ ] coverage gate 유지
- [ ] `uv run pyright` 0 errors 유지 (Phase 4 의 직전 cycle 결과)

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 추출 모듈 + main.py 슬림화
- [ ] 회귀 + pyright 0 errors
- [ ] 다음 Phase 진행 가능

---

## Phase 4. lib/api.ts 6 도메인 점진 교체

### 목표

직전 cycle 의 generated `lib/api.generated.ts` 를 실제 frontend 호출에 점진 적용. 한 번에 모든 interface 교체는 위험 — 6 도메인 우선 (`McpServiceSummary` 등) 만 generated alias 로 재선언.

### 구현 태스크

- [ ] `apps/web/lib/api.ts` 에서 6 interface 를 generated alias 로 교체
  - `export type McpServiceSummary = components["schemas"]["ServiceSummary"]` 식으로
  - `export type ToolboxSummary = components["schemas"]["ToolboxSummary"]`
  - `export type PlaygroundToolSummary = components["schemas"]["PlaygroundToolSummary"]`
  - `export type ExternalConnectionSummary = components["schemas"]["ExternalConnectionSummary"]`
  - `export type ClientTokenSummary = components["schemas"]["ClientTokenSummary"]`
  - `export type CodexSimulatorResponse = components["schemas"]["CodexSimulatorResponse"]`
- [ ] 누락 field 가 frontend 사용처에서 발견되면 lib/api.ts 의 인터페이스를 추가 (extra=allow 의 자유도)
- [ ] frontend 호출 사이트 변경 없음 — type alias 라 ABI 동일

### 자체 테스트

- [ ] `pnpm typecheck` 통과
- [ ] `pnpm build` 통과
- [ ] `pnpm lint` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 6 type alias 적용
- [ ] frontend 빌드 통과
- [ ] 다음 Phase 진행 가능

---

## Phase 5. multi-mixin Repository → 명시 facade

### 목표

`Repository` 의 8 mixin 패턴이 pyright 의 reportAttributeAccessIssue 를 양산 → `RepositoryFacades` 와 같은 명시 facade 로 cross-mixin 호출 정리. 본 cycle 은 **분석 + 부분 분리만**, 완전 전환은 next cycle.

### 구현 태스크

- [x] **ADR-046 multi-mixin → facade 마이그레이션 계획** 추가
  - 8 mixin 의 cross-mixin 호출 분석
  - 3-step 점진 전환 계획 (Jobs → Audit → 나머지 5개)
  - 각 step 의 결합도 / 마이그레이션 효과 명시
- [ ] **실제 코드 전환 → next cycle 로 분리**. 사유:
  - 본 cycle 의 8 phase 중 5번째라 컨텍스트 / 시간 예산상 무리.
  - mixin → facade 전환은 동작 보존이 까다로워 single mixin 마다 1 cycle 단위 권장.
  - ADR 만 먼저 만들어 후속 cycle 의 출발점 확정.

### 자체 테스트

- [x] `uv run pytest -q` 통과 (212 tests, 코드 변경 0)
- [x] `uv run pyright` 0 errors 유지

### 이슈 및 수정

- [x] **이슈**: 원래 계획은 `JobsRepositoryMixin` sample 전환이었으나, "동작 보존" 검증을 1 cycle 안에서 안전하게 끝내기 어려움 (각 호출 site 의 트랜잭션 boundary 영향 분석 필요). ADR 만 작성하고 코드 전환은 다음 cycle.

### 완료 조건

- [x] ADR-046 기록 + 마이그레이션 계획 확정
- [x] 회귀 + pyright 0 errors
- [x] 다음 Phase 진행 가능

---

## Phase 6. pyright `standard` mode

### 목표

`typeCheckingMode = "basic"` → `"standard"` 로 한 단계 상승. 새 warning 일부 fix.

### 구현 태스크

- [ ] `pyproject.toml` 의 `[tool.pyright] typeCheckingMode = "basic"` → `"standard"`
- [ ] `uv run pyright` 실행 → 신규 warning 카운트
- [ ] errors 0 유지, warnings 가 50 미만이면 그대로 fix, 그 이상이면 baseline 으로 reverte 후 dev-plan 에 기록

### 자체 테스트

- [ ] `uv run pyright` 0 errors
- [ ] `uv run pytest -q` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] standard mode 적용 가능 여부 확정 (적용 OR baseline 보존 사유)
- [ ] 다음 Phase 진행 가능

---

## Phase 7. frontend admin-shell / sections useReducer

### 목표

`admin-shell.tsx` 의 token-related state (mounted, token/tokenPreview 표시) 정리. dashboard-section 같이 가장 큰 section 한두 개만 useReducer 후보 시도.

### 구현 태스크

- [ ] `admin-shell.tsx` 의 `mounted` prop 처리 정리 (admin-console 의 token reducer 에서 직접 전달)
- [ ] `dashboard-section.tsx` 의 state inventory + useReducer 마이그레이션 (있다면)
- [ ] 다른 section 은 next cycle

### 자체 테스트

- [ ] `cd apps/web && pnpm typecheck && pnpm build && pnpm lint` 통과
- [ ] web admin 7 페이지 빌드 OK

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] admin-shell mount 처리 + 1 section useReducer
- [ ] 빌드 통과
- [ ] 다음 Phase 진행 가능

---

## Phase 8. Redis wire INCR/EXPIRE 실제 구현 (fakeredis)

### 목표

`RedisRateLimiter.check()` 가 진짜 redis-py 호출로 INCR + EXPIRE 를 수행. 운영 Redis 없이 검증할 수 있게 fakeredis 도입.

### 구현 태스크

- [ ] `apps/api/pyproject.toml` 에 `redis>=5.0` (optional dep) + dev `fakeredis>=2.20`
- [ ] `RedisRateLimiter.check()` — `INCR key` + `EXPIRE key window_seconds NX` 패턴, count 가 limit 초과 시 `RateLimitDecision(allowed=False, ...)` 반환
- [ ] `tests/test_redis_rate_limiter.py` 신설 — fakeredis 로 INCR/EXPIRE 검증, expiration after window
- [ ] 운영 Redis URL 미설정 시 InMemory fallback 동작 유지

### 자체 테스트

- [ ] `cd apps/api && uv run pytest tests/test_redis_rate_limiter.py -q` 통과
- [ ] `uv run pytest -q` 전체 통과
- [ ] `uv run pyright` 0 errors 유지

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] INCR/EXPIRE 구현 + fakeredis 회귀
- [ ] 회귀 + pyright 0 errors
- [ ] dev-plan 종료 — 다음 cycle 후보로 운영 Redis 연결 / Tailscale ACL 등

---

## 잔여 리스크 / 후속 과제

- 운영 Redis 환경 연결 (URL/auth/Tailscale ACL) — Phase 8 완료 후 별도 cycle
- frontend `admin-shell / sections` 의 전체 useReducer 전환 — Phase 7 은 첫 step 만
- multi-mixin Repository 의 **전체** facade 전환 — Phase 5 의 ADR-046 에 따라 점진
- pyright `strict` mode — `standard` 안정화 후 다음 단계
- `lib/api.ts` 의 잔여 (Tool / Override / Audit / etc.) interface 완전 generated 전환
