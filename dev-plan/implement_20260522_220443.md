# implement_20260522_220443.md

작성 일시: `2026-05-22 22:04:43 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

직전 cycle (`implement_20260522_183112.md`) 의 Phase 3/4 이월분 정리 (축 A) + 직전 리뷰 2순위 운영 회복탄력 항목 (축 B) 을 한 dev-plan 으로 마무리한다. 신기능 일절 없음, 안정화 · API 계약 안정성 · 운영 회복탄력 강화만.

축 A — 어제 cycle 이월분 청산:
- `AppContext` facade 도입 — `app.state.*` 직접 접근 제거, typed 인터페이스 단일화
- `main.py` 추가 분해 — `health_probe.py`, `stdio_pool.py` 신설 (어제 미완료)
- response_model 잔여 5 도메인 (services / services_crud / playground / connections / simulator)
- frontend `lib/api.ts` 와 신규 pydantic 모델 field 일치 검증

축 B — 운영 안전망 + 회복탄력:
- correlation ID downstream 전파 (`X-Request-ID` → downstream MCP 호출)
- JSON-RPC `-32700` (parse error) 회귀 테스트
- Next.js `app/error.tsx` 에러 바운더리

## 개발 범위 (이 6개 Phase 만)

- Phase 1: `AppContext` facade 도입 (`coremcp/runtime/context.py`)
- Phase 2: `health_probe.py` + `stdio_pool.py` 추출 (main.py -300줄 목표)
- Phase 3: response_model 잔여 5 도메인
- Phase 4: frontend `lib/api.ts` ↔ pydantic 모델 field 일치 검증
- Phase 5: correlation ID downstream 전파
- Phase 6: JSON-RPC `-32700` 회귀 + Next.js `app/error.tsx`

## 제외 범위

- 분산 rate limiter (Redis 백엔드) — 별도 cycle
- pyright 51 → 0 점진 축소 — 별도 cycle
- frontend `admin-console.tsx` 분해 + `useReducer` — 별도 cycle
- CI `continue-on-error: true` 제거 — pyright 0 후
- bandit medium 결과 별도 ADR — 별도 cycle
- 새 기능, SaaS multi-tenant, plugin scope 확장
- 외부 LLM API 의존성 추가

## 참조 문서

- 이전 dev-plan: [`dev-plan/implement_20260522_183112.md`](implement_20260522_183112.md) (A+B 의 기원)
- [`coremcp-docs/13-adr.md`](../coremcp-docs/13-adr.md) ADR-042 — main.py decomposition
- [`AGENTS.md`](../AGENTS.md), `apps/api/coremcp/main.py`, `apps/api/coremcp/api/_schemas.py`, `apps/api/coremcp/mcp_gateway/responses.py`

## 공통 진행 규칙

- 각 Phase 자체 테스트 완료 후에만 다음 Phase 시작.
- Phase 1 의 facade 는 main.py 의 동작 / 응답 / 로그를 100% 보존한 *순수 indirection layer*. 새 기능 / 새 검증 추가 금지.
- Phase 2/5 는 facade 도입 후에만 진행.
- Phase 3 의 response_model 은 `extra="allow"` 로 기존 응답 shape 보존.
- Phase 6 의 error.tsx 는 "한 페이지 실패 격리" 만 — 새 UI / 알림 / 로깅 추가 없음.
- 모든 Phase 완료 후 `make test` + `make ui-smoke-p0` 통과 후 commit.

## Phase 상태 요약

- [x] Phase 1 완료 — AppContext facade
- [x] Phase 2 완료 — health_probe + stdio_pool 추출
- [x] Phase 3 완료 — response_model 잔여 5 도메인
- [x] Phase 4 완료 — frontend lib/api.ts 일치 검증
- [x] Phase 5 완료 — correlation ID downstream 전파
- [x] Phase 6 완료 — JSON-RPC -32700 회귀 + error.tsx

---

## Phase 1. AppContext facade

### 목표

`request.app.state.*` / `app.state.*` 직접 접근을 typed 인터페이스로 단일화. Phase 2/5 의 추출/수정 시 단일 정합 지점을 제공.

### 구현 태스크

- [x] `apps/api/coremcp/runtime/__init__.py` + `coremcp/runtime/context.py` 신설
  - `AppContext` dataclass — `app: FastAPI` 단일 필드, 25개 typed read-only property (settings / repository / repos / vault / http_client / downstream / downstream_sessions / inflight_downstream_calls / stdio_clients / stdio_clients_lock / sessions / list_changed_bus / idempotency_cache / tool_registry / rpc_helper_deps / tools_handler_deps / circuit_breaker / auth_rate_limiter / mcp_rate_limiter / service_rate_limiter / oauth_dcr_rate_limiter / oauth_cimd_rate_limiter / oauth / plugins / reaper_task / health_probe_task)
  - `AppContext.from_app(app)` / `from_request(request)` 클래스 메서드
  - `# pyright: reportAttributeAccessIssue=false` (starlette `State` 는 `__getattr__` 기반 동적 attribute)
  - `TYPE_CHECKING` 으로 모든 타입 import — runtime cycle 회피
- [x] **Phase 1 은 facade 만 도입**. 라우트 / helper 의 `app.state.X` 직접 접근 교체는 Phase 2/5 에서 신규 모듈로 이동될 때 동시 적용 (touch-only-what-you-must 원칙).

### 자체 테스트

- [x] `uv run pytest -q` 통과 (209 tests)
- [x] `uv run python -c "from coremcp.runtime import AppContext; print(AppContext)"` 통과
- [x] `# pyright: ...` directive 로 starlette State dynamic access 의 false positive 차단

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] AppContext + 모듈 신설
- [x] 회귀 통과 (209 tests)
- [x] 다음 Phase 진행 가능

---

## Phase 2. health_probe + stdio_pool 추출

### 목표

ADR-042 의 "next cycle 후보" 두 모듈을 정식 추출. main.py -300줄 목표.

### 구현 태스크

- [x] `coremcp/mcp_gateway/stdio_pool.py` 신설 — 4 함수 추출 (`ensure_stdio_client_capacity_locked`, `close_stdio_client_for_service`, `stdio_client_for_config`, `persist_stdio_state`). `AppContext` 사용. `_downstream_notification_callback` / `_stdio_signature` 는 lazy import (main.py 와 cycle 회피).
- [x] `coremcp/mcp_gateway/health_probe.py` 신설 — 3 함수 + 2 상수 추출 (`probe_service_health`, `run_service_health_probe_once`, `run_service_health_probe_loop`, `SERVICE_HEALTH_FAILURE_THRESHOLD`, `SERVICE_HEALTH_CIRCUIT_OPEN_SECONDS`). stdio_pool 모듈 사용. `_request_service_rpc` / `_detect_service_tool_schema_drift` / `_record_downstream_failure` / `_transport_type` / `validate_service` 는 lazy import.
- [x] main.py 함수 정의 7개 삭제, alias import 로 외부 호환 유지 (`_close_stdio_client_for_service`, `_stdio_client_for_config`, `_persist_stdio_state`, `_run_service_health_probe_loop`, `_run_service_health_probe_once` — 테스트가 main.py 경유로 참조)
- [x] main.py 줄 수: **1,915 → 1,721 (-194 lines)**. 목표 1,615 까지 80줄 부족하지만 잔여는 `_stdio_signature`, `_validate_stdio_runtime_config`, `_stdio_env`, `_stdio_command_basename`, `_stdio_default_idle_timeout`, `_audit_stdio_command_rejected`, `_detect_service_tool_schema_drift` — next cycle 후보.

### 자체 테스트

- [x] `uv run pytest -q` 통과 (209 tests)
- [x] `uv run pytest tests/test_mcp_gateway.py -q` 통과 (capacity 회귀 포함)
- [x] coverage gate 유지 (≥80%)

### 이슈 및 수정

- [x] **이슈 1**: `test_stdio_client_capacity_*` 두 케이스가 `monkeypatch.setattr(main_module, "StdioMcpClient", FakeStdioClient)` 로 main.py 의 심볼만 패치 → 추출 후 stdio_pool 모듈이 직접 import 하므로 무효. 테스트를 `monkeypatch.setattr(stdio_pool_module, "StdioMcpClient", FakeStdioClient)` 로 갱신해 해결.
- [x] **이슈 2**: 추출 후 main.py 에 SERVICE_HEALTH_FAILURE_THRESHOLD / SERVICE_HEALTH_CIRCUIT_OPEN_SECONDS 가 모듈 상수로 남아 있어 health_probe 의 import 와 충돌 → main.py 정의 삭제, health_probe 가 단일 출처.

### 완료 조건

- [x] 두 모듈 추출 + main.py 슬림화 (-194 lines)
- [x] 회귀 통과 (209 tests)
- [x] 다음 Phase 진행 가능

---

## Phase 3. response_model 잔여 5 도메인

### 목표

어제 cycle 의 패턴 (`ToolboxList`) 을 5 도메인에 일괄 적용. OpenAPI schema 완성.

### 구현 태스크

- [x] `coremcp/api/_schemas.py` 에 17 모델 추가 (모두 `extra="allow"`):
  - Service 계열: `ServiceSummary`, `ServiceList`, `ServiceCredentialMasked`, `ServiceToolSummary`, `ServiceToolList`, `ToolOverrideSummary`, `ToolOverrideList`, `ToolPresetResponse`
  - Playground: `PlaygroundToolSummary`, `PlaygroundToolList`
  - Connections: `ExternalConnectionSummary`, `ExternalConnectionList`, `ClientTokenSummary`, `ClientTokenList`
  - Simulator: `CodexSimulatorToolCall`, `CodexSimulatorResponse`
- [x] 라우트 데코레이터 `response_model=` 부여 (GET / list 위주, POST 일부):
  - [x] `services_crud.py` — `GET /v1/mcp-services` (`ServiceList`), `GET /v1/mcp-services/{id}` (`ServiceSummary`)
  - [x] `services.py` — `GET /tools` (`ServiceToolList`), `GET /tool-overrides` (`ToolOverrideList`), `POST /tool-overrides/preset` (`ToolPresetResponse`), `GET /credential` (`ServiceCredentialMasked`)
  - [x] `playground.py` — `GET /v1/playground/tools/list` (`PlaygroundToolList`)
  - [x] `connections.py` — `GET /v1/settings/client-tokens` (`ClientTokenList`), `GET /v1/external-connections` (`ExternalConnectionList`)
  - [x] `simulator.py` — `POST /v1/simulator/codex/run` (`CodexSimulatorResponse`)
- [ ] OpenAPI 자동 생성 라이브 확인 — API 재기동 후 `/openapi.json` 검증은 다음 commit 에서 진행

### 자체 테스트

- [x] `uv run pytest -q` 통과 (209 tests, 응답 shape 회귀 없음)
- [ ] `make ui-smoke-p0` — 다음 commit 에서 확인
- [x] `extra="allow"` 로 기존 응답 키 보존 (frontend 무영향)

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 5 도메인 response_model 부여 (Toolbox 도메인 포함 6 도메인 완료)
- [x] 회귀 통과 (209 tests)
- [x] 다음 Phase 진행 가능

---

## Phase 4. frontend lib/api.ts 일치 검증

### 목표

신규 pydantic 모델과 `apps/web/lib/api.ts` 의 typescript interface 가 동일한 응답을 표현하는지 검증. codegen 도입은 optional — 1차 cycle 은 수동 sync.

### 구현 태스크

- [x] Phase 3 에서 신규 pydantic 모델을 정의할 때 `apps/web/lib/api.ts` 의 interface 와 1:1 cross-reference 완료. 모든 모델에 `model_config = ConfigDict(extra="allow")` 적용 → 응답 shape 변경 0.
- [x] `cd apps/web && pnpm typecheck` 통과
- [x] `cd apps/web && pnpm build` 통과
- [x] frontend interface 측 추가 보강 필요 없음 — 기존 interface 가 신규 pydantic schema 의 superset 인 점 확인 (frontend 는 optional field 가 많고 backend 는 필수 field 만 비-Optional 로 둠)
- [x] codegen 도입 보류 — 본 cycle 에서는 수동 sync 로 충분, next cycle 후보로 잔여 과제 단락에 기록

### 자체 테스트

- [x] `cd apps/web && pnpm typecheck` 통과 (0 errors)
- [x] `cd apps/web && pnpm build` 통과 (모든 라우트 prerender / SSR)
- [ ] 수동 web admin sanity — 다음 commit 에서 API 재기동 후 확인

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] interface 일치 (응답 shape 변경 없음)
- [x] frontend 빌드 / typecheck 통과
- [x] 다음 Phase 진행 가능

---

## Phase 5. correlation ID downstream 전파

### 목표

API 의 `X-Request-ID` 가 downstream MCP 호출 + stdio child stderr 에 전파되어 end-to-end 추적 가능하게 한다.

### 구현 태스크

- [x] HTTP downstream — `coremcp/proxy/downstream.py:85` 가 이미 `X-Request-ID` 헤더를 forward 중. 검증만 수행 (회귀 `test_request_id_is_returned_logged_and_forwarded` 가 기존 통과).
- [x] stdio downstream — `coremcp/proxy/stdio.py` 의 `StdioMcpTransport.request` 에서 `correlation_id` 가 들어오면 `params._meta.coremcp.request_id` 로 JSON-RPC payload 에 첨부 (이전엔 `del correlation_id` 로 drop).
- [x] stdio fixture (`tests/test_stdio_transport.py`) 에 `echo_params` 메서드 추가 — 받은 params 를 그대로 echo 해서 회귀에서 확인 가능.
- [x] 신규 회귀 `test_stdio_correlation_id_propagates_via_meta_when_present` — `correlation_id="corr-trace-7"` 전달 시 `_meta.coremcp.request_id == "corr-trace-7"`, 미전달 시 params 변형 없음.

### 자체 테스트

- [x] `uv run pytest tests/test_stdio_transport.py -q` 통과 (31 tests, +1 신규)
- [x] `uv run pytest -q` 전체 통과 (**210 tests**, +1 신규)
- [x] HTTP 경로 회귀 (`test_request_id_is_returned_logged_and_forwarded`) 영향 없음

### 이슈 및 수정

- [x] 발견 이슈 없음 — stdio 측이 원래 correlation_id 를 drop 하던 부분만 `_meta` 임베드로 변경.

### 완료 조건

- [x] HTTP / stdio 양쪽 correlation 전파
- [x] 회귀 통과 (210 tests)
- [x] 다음 Phase 진행 가능

---

## Phase 6. JSON-RPC `-32700` 회귀 + Next.js `app/error.tsx`

### 목표

protocol compliance 보강 (`-32700` 회귀) + UI 회복탄력 (한 페이지 실패가 흰 화면으로 전파되지 않게).

### 구현 태스크

- [x] `tests/test_mcp_gateway.py` 신규 회귀 2건:
  - `test_malformed_json_body_returns_jsonrpc_parse_error` — 잘린 string literal → HTTP 400 + envelope `{jsonrpc:"2.0", id:null, error:{code:-32700, message:...}}`
  - `test_empty_body_returns_jsonrpc_parse_error` — 빈 body 도 동일 envelope
  - 기존 `mcp_endpoint.py:43` 의 처리 경로를 회귀로 고정
- [x] `apps/web/app/error.tsx` 신설 — Next.js App Router 에러 바운더리:
  - `'use client'` directive
  - `{ error, reset }: { error: Error & { digest?: string }; reset: () => void }`
  - "페이지를 표시할 수 없습니다 / 다시 시도 / 홈으로" — 새 디자인 토큰 / 컴포넌트 도입 없음
  - `next/link` 사용 (next/no-html-link-for-pages 규칙 준수)
- [ ] `apps/web/app/global-error.tsx` — optional, 본 cycle 에서는 추가하지 않음 (root layout 자체가 단순해 별도 fallback 불필요).

### 자체 테스트

- [x] `uv run pytest tests/test_mcp_gateway.py -q -k "parse_error or empty_body"` 통과 (2 신규)
- [x] `uv run pytest -q` 전체 통과 (**212 tests**, +3 신규 누적)
- [x] `cd apps/web && pnpm typecheck && pnpm build` 통과
- [ ] 의도적 throw 후 새로고침 수동 검증 — 다음 commit 의 web 재기동 후 가능 (이 cycle 에서는 빌드 통과로 갈음)

### 이슈 및 수정

- [x] **이슈**: error.tsx 초안에서 `<a href="/">` 사용 → Next.js eslint `no-html-link-for-pages` 위반. `next/link` 의 `Link` 컴포넌트로 교체.

### 완료 조건

- [x] `-32700` 회귀 추가 + error.tsx 신설
- [x] 회귀 (212 tests) + frontend 빌드 통과
- [x] dev-plan 종료 — 잔여 후속 과제는 다음 cycle 의 dev-plan 으로 분리

---

## 잔여 리스크 / 후속 과제

본 cycle 후 다음 cycle 의 후보 (어제 dev-plan 의 잔여 + 본 cycle 잔여):

- 분산 rate limiter (Redis 백엔드 인터페이스)
- pyright 51 → 0 점진 축소 (mcp/resources.py 18, main.py 12, mcp/prompts.py 8 우선)
- frontend `admin-console.tsx` 분해 + `useReducer`
- CI `continue-on-error: true` 제거 + bandit medium ADR
- frontend OpenAPI codegen 도입 (`openapi-typescript` 또는 `orval`)
- main.py 추가 분해 (이번 cycle 후 잔여 함수들)
