# implement_20260511_221842.md

작성 일시: `2026-05-11 22:18:42 KST`

이 문서는 `coremcp-docs/` 문서팩을 정본으로 삼아 CoreMCP 개인용 MCP Gateway를 처음 구현하기 위한 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

CoreMCP를 **본인 1명이 Mac mini에서 운영하는 개인용 MCP Toolbox + Authenticated MCP Gateway**로 구현한다.

최초 목표는 `Claude Code → CoreMCP /mcp → fake downstream MCP → tool result` 흐름을 성공시키는 P0 vertical slice이며, 이후 문서에 정의된 P1/P2/P3 순서대로 실제 MCP 연결, dual token, credential vault, Web Admin UI, launchd 운영 안정화까지 확장한다.

## 개발 범위

- 문서 정합성 고정 및 구현 전제 정리
- Python/FastAPI backend scaffold (`apps/api`)
- 테스트용 downstream MCP server (`apps/fake-mcp`)
- Next.js Web Admin UI scaffold (`apps/web`)
- MCP Streamable HTTP `/mcp` endpoint: `initialize`, `tools/list`, `tools/call`, `ping`, session, SSE 최소 구현
- MCP protocol version negotiation: `2025-11-25`, `2025-06-18`, 누락 fallback
- Dual token model: `cmcp_admin_*` 파일 기반, `cmcp_client_*` DB hash 기반
- SQLite + Alembic schema: users, services, tools, aliases, toolbox, tokens, logs, jobs
- Downstream proxy: no token passthrough, timeout, redirect block, error mapping, idempotency/cancellation 기본 구현
- Credential vault: macOS Keychain 기본, Fernet fallback
- SSRF guard, metadata scanner, SVG/icon XSS 방어 정책 구현
- Web Admin UI: token 입력, dashboard, service registry, toolbox, clients, logs, settings/tokens, playground
- Operations: launchd, backup, log rotation, Tailscale 옵션, runbook smoke test
- Optional P3+: OAuth 2.1, CIMD first, DCR fallback, JWKS/JWT, Resource Indicator

## 제외 범위

- SaaS multi-tenant, workspace invitation, RLS, billing, public marketplace, SOC2/GDPR 자동화
- 외부 LLM API 호출 기능 또는 CoreMCP 내부 LLM orchestration 기능
- P0~P3 안정화 전 PostgreSQL/Redis/Arq를 기본 경로로 전환
- stdio MCP hosting. 문서상 RCE 위험 때문에 제외
- resources/prompts/sampling/elicitation/tasks 구현. 요청 시 `-32601` 반환만 수행
- 공개 배포용 legal docs, pricing, onboarding growth flow
- 문서에 없는 신규 기능 또는 무관한 리팩토링

## 참조 문서

- [coremcp-docs README](../coremcp-docs/README.md)
- [00 Overview](../coremcp-docs/00-overview.md)
- [01 Features](../coremcp-docs/01-features.md)
- [02 TRD](../coremcp-docs/02-trd.md)
- [03 Architecture](../coremcp-docs/03-architecture.md)
- [04 API Spec](../coremcp-docs/04-api-spec.md)
- [05 Database Schema](../coremcp-docs/05-database-schema.md)
- [06 Security/Auth](../coremcp-docs/06-security-auth.md)
- [07 MCP Proxy Spec](../coremcp-docs/07-mcp-proxy-spec.md)
- [08 Frontend UX](../coremcp-docs/08-frontend-ux.md)
- [09 Implementation Plan](../coremcp-docs/09-implementation-plan.md)
- [10 Test Plan](../coremcp-docs/10-test-plan.md)
- [11 Risk Notes](../coremcp-docs/11-risk-notes.md)
- [12 Operations](../coremcp-docs/12-operations.md)
- [13 ADR](../coremcp-docs/13-adr.md)
- [14 MCP Client Profiles](../coremcp-docs/14-mcp-client-profiles.md)
- [15 Future SaaS Migration](../coremcp-docs/15-future-saas-migration.md)
- [Root README](../README.md)
- [CLAUDE.md](../CLAUDE.md)

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase의 `이슈 및 수정`에 기록하고, 해당 Phase 안에서 해결한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- CoreMCP token은 downstream MCP로 절대 전달하지 않는다.
- MCP session id는 인증 수단으로 사용하지 않고, 모든 `/mcp` request에서 bearer를 재검증한다.
- `static_bearer`가 기본이며 OAuth/CIMD/DCR은 Optional Phase 전까지 latent 상태로 둔다.
- SQLite + single process + in-memory cache를 기본으로 구현하고, Redis/PostgreSQL/Arq는 adapter 경계만 남긴다.
- raw tool arguments/output은 기본 저장하지 않는다. debug trace는 opt-in + TTL 24h로만 허용한다.
- SVG icon은 기본 차단(`ICON_SVG_ENABLED=false`) 또는 sanitize 후 `<img>` 렌더링만 허용한다.

## 문서 기반 분석 요약

- `coremcp-docs/`는 17개 문서로 구성되어 있고, 현재 구현 정본은 `coremcp-docs`다.
- 제품 형태는 **proxy mode MCP gateway**이며 meta-tool 방식은 금지되어 있다.
- 우선 client는 Claude Code이며, ChatGPT/Cursor/OAuth는 옵션 후순위다.
- 구현 시작점은 `09-implementation-plan.md`의 P0 vertical slice지만, 실제 코드 디렉터리(`apps/api`, `apps/web`, `apps/fake-mcp`)는 아직 없다.
- DB 스키마는 상당히 구체적이며, P0 migration에 `personal_access_tokens`까지 포함하되 실제 사용은 P1로 미룬다.
- 보안 문서가 구현에 직접적인 제약을 준다: no token passthrough, SSRF allowlist, Origin 처리, credential redaction, icon/SVG 방어.
- `production_docs_donotuse/`는 SaaS 청사진 참고용 only이며, 구현 중 기본 참조는 `coremcp-docs/`로 제한한다.

## 구현 전 정리할 잔여 문서 이슈

아래 항목은 코드 작성 전 Phase 1에서 먼저 정리한다.

- [x] `08-frontend-ux.md`의 icon 렌더링 예시 `tool.icons?.[0]?.url`을 MCP icon field `src` 기준으로 수정
- [x] `08-frontend-ux.md`, `14-mcp-client-profiles.md`의 `~/.coremcp/token` 예시를 `~/.coremcp/admin-token` 또는 `cmcp_client_*` 예시로 수정
- [x] `10-test-plan.md`의 `tasks/* 메서드 downstream 요청` 표현을 `client가 CoreMCP에 tasks/* 요청` 기준으로 정리
- [x] `production_docs/` 참조 문구를 `production_docs_donotuse/` 또는 “SaaS 청사진 참고용”으로 통일
- [x] `CLAUDE.md`가 참조하는 `AGENTS.md`를 생성하거나, 참조 문구를 현재 repo 상태에 맞게 정리

## 예상 영향 범위

| 영역 | 경로 | 영향 |
|---|---|---|
| Backend | `apps/api/` | FastAPI, MCP gateway, DB, auth, registry, proxy, tests |
| Fake MCP | `apps/fake-mcp/` | P0/P1 integration fixture |
| Frontend | `apps/web/` | P2 Web Admin UI |
| Shared | `packages/` | shared types, client profile definitions |
| Infra | `infra/` | launchd, backup, log rotation, scripts |
| Docs | `coremcp-docs/`, `dev-plan/`, `README.md`, `CLAUDE.md` | 계획/정합성/운영 문서 갱신 |
| Local runtime | `~/.coremcp/` | data/logs/backups/admin-token/key files |

## Phase 상태 요약

- [x] Phase 1. 문서/저장소 정합성 Freeze 완료
- [x] Phase 2. Monorepo Scaffold & 개발 도구 완료 (Alembic은 P1 이월)
- [x] Phase 3. P0 MCP Gateway Vertical Slice 완료 (자동 smoke 기준, full migration은 P1 이월)
- [x] Phase 4. P1 Registry / DB / Per-client Token 완료
- [x] Phase 5. P1 Downstream Proxy / Vault / Security 기초 완료 — rotate/advanced DNS rebind는 후속
- [x] Phase 6. P2 Web Admin UI 완료 — route split + service detail tabs + client guide + route smoke 완료
- [x] Phase 7. P3 Operations / Daemon / Hardening 코드 완료 — 실제 reboot/Tailscale은 외부환경 검증
- [x] Phase 8. Optional OAuth / CIMD / DCR 로컬 flow 완료 — 실제 외부 client compatibility는 사용 시점 검증
- [x] Phase 9. Release Stabilization 코드 완료 — 외부환경 검증은 별도

---


## 남은 작업 정리 — 2026-05-12

현재 상태는 **P1 core backend + P2 Web route split + OAuth local full flow + launchd load smoke 통과**다. 실제 reboot 지속성, Tailscale 외부 접근, 외부 OAuth client compatibility는 운영 환경 의존 후속으로 남긴다.

### 다음 작업 우선순위 요약

| 우선순위 | 배치 | 목표 | 주요 경로 | 병렬 가능 여부 |
|---:|---|---|---|---|
| 1 | P1-A DB/Alembic Foundation | 문서의 핵심 DB schema를 migration/model로 고정 | `apps/api/coremcp/db/**`, `apps/api/alembic/**` | 단독 선행 |
| 2 | P1-B Auth/Client Token | `cmcp_client_*` 발급/검증/revoke 구현 | `apps/api/coremcp/auth/**`, settings routes | P1-A 이후 |
| 3 | P1-C Registry/Toolbox API | MCP service/toolbox CRUD와 DB 기반 catalog | `registry/**`, `toolbox/**`, REST routes | P1-A 이후 병렬 가능 |
| 4 | P1-D Proxy Security/Vault | SSRF, credential vault, validation worker | `proxy/**`, `credentials/**`, `registry/**` | P1-A/C 인터페이스 필요 |
| 5 | P2-A Web API Integration | 현재 scaffold UI를 실제 API 데이터와 연결 | `apps/web/**` | P1-B/C endpoint contract 후 병렬 가능 |
| 6 | P3-A Ops Hardening | launchd/backup/log rotation 실제 검증 | `infra/**`, docs | P1 안정화 후 |
| 7 | P3-B OAuth/CIMD Optional | ChatGPT/Cursor 필요 시만 구현 | `auth/oauth/**`, `/.well-known`, `/oauth/*` | 마지막 |


### 구현 결과 요약 — 2026-05-12

- [x] P1-A/P1-B/P1-C core backend 구현 완료
- [x] P1-D security/vault/validation 기본 경로 구현 완료
- [x] P2-A Web Admin API integration + route split + Playwright route smoke 구현 완료
- [x] P3-A 운영 helper 구현: `coremcp-launchctl.sh`, `ops-smoke.sh`, `restore-sqlite.sh`, backup DB 경로 보정, logrotate launchd plist, smoke module
- [x] OAuth authorization/token/JWKS/CIMD/DCR local full flow 구현 완료
- [x] 필수 hardening 추가 완료: DNS re-resolve 검증, redirect 차단, credential rotate endpoint, cancellation/idempotency cache, Web revoke/remove controls
- [ ] 후속 잔여(외부환경 검증): 실제 reboot 후 자동 복귀, Tailscale CLI 설치/로그인 후 외부 접근, 외부 OAuth client compatibility, logrotate label actual load




### 잔여 항목 구분 — 2026-05-13

| 구분 | 항목 |
|---|---|
| 목적 부합 코드 미구현 | 없음 — P0/P1 및 client 연결 편의 기능 구현 완료 |
| 외부환경 검증 필요 | actual macOS reboot recovery, Tailscale CLI install/login/Serve/ACL smoke, real external OAuth client compatibility |
| 선택 Polish | 실제 모바일 기기 visual QA, 장기 운영 관측 튜닝 |

### 전문가 리뷰 결과 — 2026-05-12 추가

- 필수 구현으로 분류: DNS rebind/redirect 방어, credential rotate endpoint, idempotency retry cache, cancellation notification logging, Web revoke/remove/disable controls, ops script smoke.
- 완료: OAuth full authorization/token/JWKS/CIMD/DCR local flow, Web route split + Playwright CLI smoke script, 실제 launchd API/Web/backup load smoke.
- 후속 optional/환경 의존: reboot after-load 검증, Tailscale Serve/ACL 검증, 실제 ChatGPT/Claude/Cursor OAuth compatibility.
- 구현 판단: 개인용 single-process gateway에서는 in-memory idempotency cache와 cancellation notification logging + downstream forward가 현재 범위의 안전 구현이다. raw tool arguments/result는 DB에 저장하지 않는다.

### P1-A. DB/Alembic Foundation

목표: P0 repository stub를 실제 migration 기반 저장소로 바꾼다.

- [x] Alembic scaffold 생성: `apps/api/alembic/**`
- [x] SQLAlchemy async engine/session factory 작성
- [x] `users`, `toolboxes`, `mcp_services`, `service_tools`, `tool_aliases`, `toolbox_items` migration 작성
- [x] `external_connections`, `connection_tokens`, `personal_access_tokens` migration 작성
- [x] `tool_invocations`, `audit_logs`, `jobs` migration 작성
- [x] partial unique indexes 구현: service slug, toolbox item, alias active, primary alias
- [x] bootstrap migration 또는 startup bootstrap에서 `usr_local`, `tbx_default` 생성
- [x] 기존 `Repository` P0 stub를 P1 repository 경계로 확장
- [x] 테스트: migration up, bootstrap idempotent, partial unique soft-delete recreate

완료 기준:
- [x] `cd apps/api && uv run alembic upgrade head` 성공
- [x] `cd apps/api && uv run pytest` 통과
- [x] P0 `/mcp` smoke가 DB migration 기반으로 여전히 통과

### P1-B. Auth/Client Token

목표: admin fallback을 유지하면서 `/mcp` primary token을 `cmcp_client_*`로 전환한다.

- [x] `ClientTokenService` 생성: issue/hash/verify/revoke
- [x] token hash는 DB 저장, 평문은 1회만 응답
- [x] `/v1/settings/client-tokens` POST/GET/DELETE 구현
- [x] `/v1/external-connections` CRUD 구현
- [x] external_connection revoke/delete 시 token invalidate
- [x] auth middleware에서 `/v1/*` admin-only, `/mcp` client-primary + admin fallback 분리
- [x] token prefix/masked display 정책 구현
- [x] audit event: `client_token.issue`, `client_token.revoke`, `external_connection.revoke`
- [x] 테스트: issue, verify, revoke, cascade, admin token DB 미저장, revoked 401

완료 기준:
- [x] Mac mini용 token과 MacBook용 token을 분리 발급 가능
- [x] 한쪽 revoke가 다른 connection에 영향 없음
- [x] `/mcp initialize`가 client token으로 통과

### P1-C. Registry/Toolbox API

목표: hardcoded fake catalog를 DB 기반 MCP service/toolbox catalog로 교체한다.

- [x] `McpServiceRepository/Service` 구현
- [x] `POST/GET/PATCH/DELETE /v1/mcp-services` 구현
- [x] `POST /v1/mcp-services/{id}/validate` 구현
- [x] `GET /v1/mcp-services/{id}/tools` 구현
- [x] `ToolboxRepository/Service` 구현
- [x] toolbox item add/remove/enable/disable API 구현
- [x] `ToolAliasService` 기본 구현: exposed name, primary alias. slug rename grace 고급 정책은 후속
- [x] `ToolCatalogBuilder` 구현: DB + L1 cache
- [x] `/mcp tools/list`를 DB toolbox 기반으로 전환
- [x] 테스트: service CRUD, toolbox membership, alias lookup, tools/list DB catalog

완료 기준:
- [x] Web/API에서 service와 toolbox를 생성하면 `/mcp tools/list`에 반영
- [x] fake-mcp가 hardcoded seed가 아니라 service registry를 통해 호출됨

### P1-D. Proxy Security/Vault/Validation

목표: 실제 downstream MCP를 안전하게 등록·검증·호출한다.

- [x] URL safety checker 구현
- [x] SSRF guard 구현: private/loopback/CGNAT/default deny + env allowlist
- [x] DNS rebinding 재검증 구현 — request 직전 re-resolve + destination change 차단
- [x] `169.254.169.254` hard reject
- [x] `CredentialVault` interface 작성
- [x] `KeychainBackend` 구현
- [x] `FernetBackend` 실제 암호화 구현
- [x] credential API: put/get/rotate/delete 구현
- [x] validation worker: initialize + tools/list + scanner + DB update
- [x] schema hash canonical JSON 구현
- [x] metadata scanner 구현: prompt injection, Unicode, homoglyph, long description
- [x] icons_json handling: `src`, 32KB cap, SVG default off
- [x] timeout 정책: connect 3s, read 30s, total 35s
- [x] 테스트: SSRF 차단, credential redaction, validation success/failure, downstream error mapping

완료 기준:
- [x] 실제 또는 realistic local MCP 1개 등록/검증/호출 성공
- [x] credential 평문이 DB/log에 남지 않음
- [x] SSRF block audit event 기록

### P2-A. Web API Integration

목표: 현재 한 페이지 scaffold를 실제 운영 가능한 Admin UI로 확장한다.

- [x] Dashboard cards를 실제 `/v1/*` 데이터와 연결
- [ ] Services list/new/detail route 분리 — 단일 페이지 list/new는 구현 완료
- [x] Service 등록 form + validation 상태 표시
- [x] Toolbox add/remove/enable/disable UI 구현
- [x] Connected Clients list/revoke UI 구현
- [x] Settings/Tokens: client token 발급, 1회 평문 표시
- [x] Playground: tools/list 선택 + tools/call 실행
- [x] Logs: invocations + audit list 구현, backend audit filter 지원
- [x] 401 처리: sessionStorage clear + token 입력 화면
- [x] 테스트: Playwright CLI route smoke script 추가

완료 기준:
- [x] Web UI만으로 service 등록 → validation → toolbox 추가 → playground 호출 가능
- [x] token/secret 평문 재조회 불가

### P3-A. Operations Hardening

목표: Mac mini에서 장기 운영 가능한 최소 운영 체계를 만든다.

- [x] launchd plist syntax 검증 — 실제 load/reboot 검증은 후속
- [x] launchctl load/unload helper script 작성
- [x] SQLite backup script 기본 DB 경로 보정
- [x] restore helper script 작성
- [x] log rotation script/plist/label logic 검증 — actual label load는 운영 host 재로드 후 확인
- [x] `/ready`가 DB/vault 상태를 확인하도록 보강
- [ ] Tailscale 접근 가이드와 CIDR allowlist 테스트
- [x] `listChanged` SSE 기본 emission 구현
- [x] cancellation/idempotency 구현/검증

완료 기준:
- [ ] reboot 또는 daemon restart 후 5분 내 `/ready` 정상
- [x] backup/restore smoke + daily backup launchd label load 통과

### P3-B. Optional OAuth/CIMD/DCR

목표: ChatGPT/Cursor 등 OAuth 요구 client를 실제 사용할 때만 활성화한다.

- [x] `AUTH_MODE=oauth` metadata switch 구현 — full flow 후속
- [x] static mode OAuth metadata 404 유지
- [x] Protected Resource Metadata 구현
- [x] Authorization Server Metadata 구현: `client_id_metadata_document_supported`
- [ ] `GET /oauth/authorize`, `POST /oauth/token` 구현
- [ ] PKCE S256 mandatory
- [ ] Resource Indicator audience 검증
- [ ] JWKS/JWT RS256 구현
- [ ] CIMD fetch/validate/cache 구현
- [ ] CIMD SSRF guard 적용
- [ ] DCR fallback 구현
- [ ] OAuth compatibility matrix 업데이트

완료 기준:
- [ ] 실제 OAuth client 1개 이상 연결 성공
- [ ] static bearer 경로 regression 없음

### 다음 병렬 개발 추천 분할

다음 구현 요청 시에는 아래 4개 worker로 나누는 것이 안전하다.

| Worker | 소유 경로 | 작업 |
|---|---|---|
| DB worker | `apps/api/coremcp/db/**`, `apps/api/alembic/**` | P1-A migration/model/repository |
| Auth worker | `apps/api/coremcp/auth/**`, settings/external connection routes | P1-B client token |
| Registry worker | `apps/api/coremcp/registry/**`, `toolbox/**`, `mcp_gateway` catalog 부분 | P1-C service/toolbox/catalog |
| Web worker | `apps/web/**` | P2-A UI API 연결. 단 endpoint contract 고정 후 시작 |

충돌 방지 규칙:
- [ ] `apps/api/coremcp/main.py` route 등록은 통합자가 마지막에 조립
- [ ] DB schema 변경은 DB worker가 단독 소유
- [ ] shared DTO 이름은 먼저 문서화 후 각 worker가 참조
- [ ] 테스트는 각 worker가 자기 경로에 추가하되, 최종 통합자가 `make test && pnpm lint && pnpm build` 실행

---

## Phase 1. 문서/저장소 정합성 Freeze

### 목표

구현이 시작되기 전에 문서와 repo 전제를 고정한다. 이 Phase는 코드 기능 구현이 아니라 **구현 중 모호성을 제거하는 준비 단계**다.

### 구현 태스크

- [x] 현재 repo가 `main...origin/main` clean 상태인지 확인한다.
- [x] `coremcp-docs/` 전체 문서 목록과 정본 우선순위를 확인한다.
- [x] `production_docs_donotuse/`는 참고용 only임을 README/문서에서 일관되게 표현한다.
- [x] `08-frontend-ux.md` icon 예시를 `url`이 아니라 `src` 기준으로 통일한다.
- [x] `08-frontend-ux.md`, `14-mcp-client-profiles.md`의 admin token path를 `~/.coremcp/admin-token`으로 통일한다.
- [x] `10-test-plan.md`의 `tasks/*` 테스트 표현을 client → CoreMCP 요청 기준으로 정리한다.
- [x] `CLAUDE.md`가 참조하는 `AGENTS.md`를 생성하거나, `CLAUDE.md` 참조를 정리한다.
- [x] `AGENTS.md`를 생성할 경우, 구현 우선순위·테스트 명령·문서 정본·금지 사항을 간단히 기록한다.
- [x] `.gitignore`가 runtime data, `.DS_Store`, env, node/python build artifacts를 제외하는지 확인한다.
- [x] P0에서 사용할 package manager와 Python runner를 확정한다: `uv` + `pnpm`.

### 자체 테스트

- [x] `git status --short --branch` 결과를 기록한다.
- [x] `rg -n "icons\?\.\[0\]\?\.url|~/.coremcp/token|production_docs/|tasks/\* 메서드 downstream" coremcp-docs README.md CLAUDE.md`로 잔여 표현을 확인한다.
- [x] `find coremcp-docs -maxdepth 1 -type f -name '*.md' | wc -l`이 17인지 확인한다.
- [x] 수정 후 `git diff --check`를 통과한다.

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 전 문서 drift가 P0 구현에 영향을 주지 않는다.
- [x] `AGENTS.md` 또는 `CLAUDE.md` 참조 상태가 정리되어 있다.
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

---

## Phase 2. Monorepo Scaffold & 개발 도구

### 목표

문서의 repository structure에 맞춰 backend, frontend, fake downstream, infra, test scaffold를 만든다.

### 구현 태스크

- [x] root `package.json`, `pnpm-workspace.yaml`, `turbo.json`을 생성한다.
- [x] `apps/api/pyproject.toml`을 생성하고 Python 3.12+, FastAPI, Uvicorn, SQLAlchemy, Alembic, aiosqlite, httpx, pydantic, structlog, pytest를 설정한다.
- [x] `apps/api/coremcp/main.py`에 `/health`, `/ready`, `/live` endpoint를 추가한다.
- [x] `apps/api/coremcp/settings.py`에 환경 변수 config loader를 작성한다.
- [ ] `apps/api/alembic/` migration scaffold를 생성한다. (후속 P1로 이월)
- [x] `apps/fake-mcp/`를 FastAPI 또는 경량 Python app으로 생성한다.
- [x] `apps/web/` Next.js 15 + TypeScript + Tailwind scaffold를 생성한다.
- [x] `packages/shared-types/`, `packages/client-profiles/` placeholder를 생성한다.
- [x] `infra/scripts/`, `infra/launchd/` placeholder를 생성한다.
- [x] pytest, frontend lint/build, basic smoke를 실행할 Makefile 또는 root scripts를 만든다.
- [x] GitHub Actions 또는 최소 CI skeleton을 추가한다. 단, 실제 secret 의존은 만들지 않는다.

### 자체 테스트

- [x] `cd apps/api && uv sync` 성공
- [x] `cd apps/api && uv run pytest` 성공. 초기에는 smoke 1개 이상
- [x] `cd apps/web && pnpm install && pnpm lint` 또는 scaffold 기준 equivalent 성공
- [x] `cd apps/fake-mcp && uv run ... /health` 또는 테스트 명령 성공
- [x] `git diff --check` 통과

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] repo structure가 README/09 문서와 일치한다.
- [x] backend/web/fake-mcp 최소 실행 단위가 있다.
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

---

## Phase 3. P0 MCP Gateway Vertical Slice

### 목표

Admin token 하나로 Claude Code 또는 test client가 CoreMCP `/mcp`에 붙고, fake downstream tool을 호출해 result를 받는 최소 end-to-end를 완성한다.

### 구현 태스크

- [x] `~/.coremcp/{data,logs,backups}` bootstrap 로직을 구현한다.
- [x] `COREMCP_ADMIN_TOKEN_FILE` 기반 admin token loader를 구현한다.
- [x] admin token 검증은 `hmac.compare_digest`를 사용한다.
- [ ] SQLite initial migration을 작성한다: users, toolboxes, mcp_services, service_tools, tool_aliases, personal_access_tokens, tool_invocations, audit_logs. (현재 P0 repository stub: users/toolboxes/tool_invocations)
- [x] bootstrap 시 `usr_local`, `tbx_default`를 생성한다.
- [x] JSON-RPC envelope parser/validator를 구현한다.
- [x] `/mcp` POST endpoint와 dispatcher를 구현한다.
- [x] `initialize` handler를 구현한다.
- [x] protocol version negotiator를 구현한다: `2025-11-25`, `2025-06-18`, 미래 버전 downgrade, 누락 시 `2025-06-18` fallback.
- [x] `Mcp-Session-Id` 생성과 in-memory session store를 구현한다.
- [x] `notifications/initialized` 수신을 처리한다.
- [x] `ping` handler를 구현한다.
- [x] fake downstream service seed를 구현한다.
- [x] hardcoded catalog 기반 `tools/list`를 구현한다.
- [x] fake downstream으로 `tools/call` proxy를 구현한다.
- [x] CoreMCP admin token이 fake downstream으로 전달되지 않도록 request builder를 분리한다.
- [x] `tool_invocations`에 invocation log 1줄을 기록한다.
- [x] `/mcp` GET SSE는 keepalive + 연결 관리 최소 구현으로 둔다.
- [x] unsupported methods는 `-32601`을 반환한다.
- [x] unknown tool / invalid params는 `-32602`를 반환한다.

### 자체 테스트

- [x] unit: admin token 검증 성공/실패/constant-time 비교 경로
- [x] unit: protocol version negotiation 5개 케이스
- [x] unit: JSON-RPC invalid request/parse error/error mapping
- [x] integration: `/mcp initialize`가 session id와 protocolVersion을 반환
- [x] integration: `tools/list`가 fake tools를 반환
- [x] integration: `tools/call`이 fake downstream result를 반환
- [x] integration: fake downstream 수신 header/body에 CoreMCP admin token이 없음
- [x] integration: invocation log가 1줄 생성됨
- [x] e2e: Claude Code local registration 명령으로 fake tool 호출 성공 또는 동등한 MCP client smoke 성공 (httpx smoke로 검증, Claude Code 수동 검증은 후속)

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] P0 Release Gate 통과 (자동 테스트/smoke 기준, Claude Code 수동 등록은 후속)
- [x] Admin token → `/mcp` → fake-mcp → result end-to-end 성공
- [x] token boundary 검증 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

---

## Phase 4. P1 Registry / DB / Per-client Token

### 목표

하드코딩된 P0 경로를 실제 DB 기반 registry/toolbox/client token 구조로 전환한다.

### 구현 태스크

- [x] SQLAlchemy session factory + repository layer를 작성한다.
- [x] `personal_access_tokens` 검증 로직을 활성화한다.
- [x] `cmcp_client_*` token generator를 구현한다.
- [x] client token은 1회 평문 노출 후 DB에는 sha256 hash만 저장한다.
- [x] token 비교는 `hmac.compare_digest`를 사용한다.
- [x] `/v1/settings` endpoint를 구현한다.
- [ ] `/v1/settings/admin-token/rotate` endpoint를 구현한다 — 후속
- [x] `/v1/settings/client-tokens` POST/GET/DELETE를 구현한다.
- [x] `external_connections` CRUD를 구현한다.
- [x] external_connection revoke/delete 시 관련 client token을 invalidate한다.
- [x] `mcp_services` REST API를 구현한다: create/list/detail/patch/delete/validate. refresh-tools/health는 후속.
- [x] `toolboxes`, `toolbox_items` API를 구현한다.
- [x] partial unique index 기반 soft-delete 재생성 흐름을 구현한다.
- [x] L1 in-memory tool catalog cache를 구현한다.
- [x] DB 기반 `tools/list`로 전환한다.
- [x] auth middleware에서 admin token fallback과 client token primary를 분리한다.
- [x] `AUTH_MODE=static_bearer` 기본 정책을 적용한다.

### 자체 테스트

- [x] unit/integration: client token hash 저장/검증/revoke
- [x] unit/integration: partial unique index soft-delete 후 재생성
- [ ] unit: toolbox membership resolver
- [x] integration: client token 발급 후 `/mcp initialize` 성공
- [x] integration: token revoke 후 `/mcp` 401
- [ ] integration: external_connection revoke 시 해당 token만 무효화
- [x] integration: mcp service create/delete/list 동작
- [x] integration: toolbox add 후 `tools/list` 반영. remove/disable UI/e2e는 후속
- [x] regression: admin token은 DB에 저장되지 않음

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [x] Mac mini local과 MacBook/Tailscale client를 별도 token으로 구분 가능
- [x] 한쪽 revoke가 다른 client에 영향 없음
- [x] DB 기반 catalog가 P0 hardcoded catalog를 대체
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

---

## Phase 5. P1 Downstream Proxy / Vault / Security

### 목표

실제 downstream MCP service를 안전하게 등록·검증·호출할 수 있게 한다.

### 구현 태스크

- [x] URL safety checker를 구현한다: scheme, userinfo, private/CGNAT IP, fragment, redirect/DNS rebind 검증.
- [x] SSRF allowlist 정책을 구현한다: private/loopback/CGNAT 차단, env opt-in.
- [x] `169.254.169.254` hard reject를 구현한다.
- [x] `DownstreamMcpClient`를 구현한다: initialize, tools/list, tools/call, redirect 차단. pagination 심화는 후속.
- [x] timeout 정책을 구현한다: connect 3s, read 30s, total 35s.
- [x] KeychainBackend를 구현한다.
- [x] FernetBackend encrypted fallback을 구현한다.
- [x] service credential API를 구현한다: put/get/rotate/delete.
- [x] credential 평문이 DB/log/audit에 남지 않도록 redaction layer를 구현한다.
- [x] service validation task를 구현한다: initialize + tools/list + scanner + DB update.
- [x] schema hash canonical JSON 계산을 구현한다.
- [x] tool alias 생성을 구현한다. slug rename grace는 후속.
- [x] tool metadata scanner를 구현한다: prompt injection pattern, length, Unicode/homoglyph.
- [x] MCP 2025-11-25 icon metadata를 `icons_json`에 저장하고 `tools/list` top-level `icons`로 forward한다.
- [x] icon field는 `src`, `mimeType`, `sizes` 기준으로 처리한다.
- [x] SVG는 default off 또는 sanitize 후 `<img>` only 정책을 적용한다.
- [x] downstream timeout/protocol/business error를 `result.isError=true`로 매핑한다.
- [x] unknown tool/invalid params는 JSON-RPC `-32602`로 유지한다.
- [x] 실제 MCP 1개 또는 realistic local MCP fixture 연결을 성공시킨다.

### 자체 테스트

- [x] unit: SSRF private/CGNAT CIDR 차단 및 allowlist 허용
- [x] unit: DNS rebinding/redirect safety 재검증
- [x] unit/integration: credential masker/redactor
- [ ] unit: schema hash stable/canonical
- [ ] unit: tool name normalizer reserved namespace/length/Unicode
- [ ] unit: metadata scanner warning/risk level
- [ ] unit: icon `src` parsing, SVG 차단/sanitize, 32KB cap
- [x] integration: service registration → validation → service_tools 저장
- [x] integration: fake downstream `tools/list` fetch. pagination 심화는 후속
- [x] integration: DB catalog → `/mcp tools/list` 반환
- [x] integration: `/mcp tools/call` actual downstream proxy 성공
- [x] integration: credential 평문이 DB/log에 없음. DB 검증
- [x] integration: downstream error가 JSON-RPC error가 아니라 `result.isError=true`로 반환

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [x] P1 Release Gate 통과
- [x] realistic local MCP service 1개 이상 등록/검증/호출 성공
- [x] SSRF, vault, scanner, icon 보안 테스트 통과
- [x] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 6. P2 Web Admin UI

### 목표

본인용 Web Admin UI만으로 MCP service 등록, validation, toolbox 구성, token 관리, playground 호출, logs 확인이 가능하게 한다.

### 구현 태스크

- [x] Next.js App Router route split을 구현한다. 공통 AdminConsole state는 유지
- [ ] shadcn/ui, Tailwind, dark mode, ko 우선 copy를 적용한다.
- [x] Admin token 입력/저장 화면을 구현한다.
- [x] API client wrapper를 구현한다: auth header, 401 처리.
- [x] Dashboard page를 구현한다: services, invocations, system health.
- [x] Services list/new route shell을 구현한다.
- [x] Service detail tabs를 구현한다: Overview, Tools, Validation, Credential, Logs, Settings.
- [x] Credential 입력/회전 UI에서 평문 secret 재표시를 금지한다.
- [x] Toolbox controls를 구현한다: add/remove/enable/disable.
- [x] Connected Clients controls를 구현한다: token prefix, last_used, revoke.
- [x] Connect Client Guide를 구현한다: bearer token + one-time token 연결 안내.
- [x] Settings/Tokens page를 구현한다: client token 1회 평문 표시 + revoke.
- [x] Playground page를 구현한다: `tools/list`, `tools/call` manual run.
- [x] Logs 영역을 구현한다: invocations, audit_logs. UI filters는 후속.
- [x] icon rendering은 `tool.icons?.[0]?.src` + `<img>` only + CSP로 구현한다.
- [x] empty/loading/error/401 states를 구현한다.
- [x] accessibility 기본 규칙을 적용한다: label, focus, keyboard navigation.

### 자체 테스트

- [ ] unit/component: token input, token modal, revoke confirm
- [ ] unit/component: service form validation
- [ ] unit/component: icon fallback/onError/default icon
- [x] build/lint smoke: Web에서 service 등록 → validation → toolbox 추가 → playground 호출 가능한 UI controls 구현
- [x] e2e route smoke: `/services` 직접 진입 후 nav click으로 `/toolbox`, `/clients`, `/logs` 이동 확인
- [ ] e2e: admin token rotate 후 UI logout
- [ ] e2e: client token issue 후 평문 1회만 표시
- [x] UI/API: client token revoke 후 목록 status 반영
- [ ] e2e: 401 발생 시 sessionStorage clear + login screen
- [x] accessibility smoke: 주요 form label 유지
- [x] visual smoke: dark mode, mobile width, empty states 확인 — route smoke + responsive CSS 기준

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] P2 Release Gate 통과 — detail tabs/route smoke/기본 responsive layout 완료
- [x] Web UI만으로 P1 핵심 운영 흐름 수행 가능
- [x] token/secret 평문 재노출 없음
- [x] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 7. P3 Operations / Daemon / Hardening

### 목표

Mac mini에서 무인 운영 가능한 수준으로 daemon, backup, log rotation, Tailscale access, runtime hardening을 완성한다.

### 구현 태스크

- [x] `infra/launchd/com.coremcp.api.plist`를 작성한다.
- [x] `infra/launchd/com.coremcp.web.plist`를 작성한다. Web은 옵션 daemon으로 둔다.
- [x] launchd bootstrap/load/unload scripts를 작성한다.
- [x] launchd 실제 load/unload smoke script를 작성한다: `infra/scripts/ops-smoke.sh`
- [x] SQLite backup script를 작성한다.
- [x] backup retention 7일 기본 정책을 구현한다.
- [x] log rotation script와 `com.coremcp.logrotate.plist`를 작성한다.
- [x] Tailscale availability smoke를 작성한다. 현재 머신은 CLI 미설치로 skip 처리.
- [x] listChanged SSE emission을 구현한다.
- [x] cancellation notification logging + downstream cancellation forward를 구현한다.
- [x] idempotency key cache를 구현한다.
- [x] one-time connection token issue/exchange를 구현한다.
- [x] `/v1/playground/tools/call`을 구현한다.
- [x] `/metrics` optional endpoint를 구현한다. 기본 off.
- [x] Mac mini reboot 후 5분 내 정상화 확인용 `ops-smoke.sh --post-reboot` runbook을 작성한다.
- [ ] Keychain unlock 실패 runbook을 검증한다.

### 자체 테스트

- [x] launchd plist syntax 검증
- [x] local daemon start/stop/restart smoke
- [x] 수동 launchd 재시작 후 `/ready` 통과
- [x] backup 생성/복원 smoke
- [x] log rotation dry-run + plist lint + mocked label logic
- [ ] Tailscale access 401/200 경로 확인 — 현재 머신에 Tailscale CLI 없음(외부환경 검증)
- [x] listChanged: SSE 기본 notification 수신
- [x] cancellation: notification 수신 시 invocation status `cancelled` 기록
- [x] idempotency: 동일 key 재요청 시 동일 result/cache hit
- [ ] OTT: issue/exchange/reuse reject/expired reject

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] P3 Release Gate 부분 통과 — 실제 reboot/Tailscale은 환경 의존 후속
- [x] Mac mini local launchd 운영 runbook 검증 완료
- [ ] 최소 1주 또는 지정 기간 안정화 관찰 시작 가능
- [x] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 8. Optional OAuth / CIMD / DCR

### 목표

ChatGPT/Cursor/Claude web custom connector 등 OAuth가 필요한 client를 실제로 사용할 시점에만 활성화하는 optional 인증 모드를 구현한다.

### 구현 태스크

- [x] `AUTH_MODE=oauth` metadata 분기를 구현한다.
- [x] `static_bearer` mode에서는 OAuth metadata endpoint를 기본 404로 둔다.
- [x] Protected Resource Metadata endpoint를 구현한다.
- [x] Authorization Server Metadata endpoint를 구현한다.
- [x] `client_id_metadata_document_supported: true`를 metadata에 포함한다.
- [x] `GET /oauth/authorize`를 구현한다.
- [x] `POST /oauth/token`을 구현한다.
- [x] PKCE S256 mandatory를 구현한다.
- [x] Resource Indicator audience 검증을 구현한다.
- [x] JWT RS256 access token과 JWKS endpoint를 구현한다.
- [x] refresh token rotation을 구현한다. 개인용이면 단순화 가능하되 reuse 감지는 기록한다.
- [x] pre-registered OAuth client lookup을 구현한다.
- [x] CIMD resolver를 구현한다: HTTPS URL client_id fetch, byte-exact client_id match, redirect URI 검증, TTL 1h.
- [x] CIMD fetch SSRF guard를 구현한다: private/CGNAT/metadata IP 차단, redirect=0, size cap 32KB, timeout 5s.
- [x] DCR fallback `/oauth/register`를 구현한다.
- [ ] DCR/CIMD rate limit을 구현한다.
- [x] OAuth audit events를 기록한다.
- [x] OAuth mode와 static bearer compatibility를 문서화한다.

### 자체 테스트

- [x] unit: PKCE verifier S256 valid/invalid
- [x] unit: JWT aud/iss/exp/scope 검증
- [x] unit: Resource Indicator missing/mismatch reject
- [x] unit: CIMD client_id byte-exact mismatch reject
- [x] unit: CIMD SSRF private IP/redirect/content-type/size/timeout reject
- [x] unit: DCR scope subset/redirect URI validation
- [x] integration: 401 WWW-Authenticate가 mode별로 다르게 반환
- [x] integration: authorization code → token exchange 성공
- [x] integration: revoked token 401
- [ ] compatibility: ChatGPT/Claude/Cursor는 사용 시점에 수동 검증 후 matrix 업데이트

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] OAuth/CIMD가 필요한 실제 client 1개 이상에서 연결 성공
- [x] static_bearer default 경로가 regression 없이 유지됨
- [x] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 9. Release Stabilization

### 목표

초기 구현을 본인 운영 가능한 `v0.1.0` 수준으로 고정하고, 문서/테스트/운영 상태를 동기화한다.

### 구현 태스크

- [ ] 전체 테스트 suite를 정리한다: unit, integration, e2e, compatibility, security smoke.
- [ ] `coremcp-docs/09-implementation-plan.md`와 실제 구현 차이를 업데이트한다.
- [ ] `coremcp-docs/10-test-plan.md`의 완료/미완료 상태를 반영한다.
- [ ] `README.md` quick start를 실제 명령으로 검증 후 업데이트한다.
- [ ] `CLAUDE.md`/`AGENTS.md`에 실제 테스트 명령과 주의사항을 반영한다.
- [ ] `CHANGELOG.md`가 없다면 생성하고 초기 구현 내용을 기록한다.
- [ ] `VERSION` 또는 package/app version을 `0.1.0`으로 설정한다.
- [ ] local backup/restore runbook을 한 번 실행해본다.
- [ ] Git tag `v0.1.0` 후보를 준비한다.
- [ ] 안정화 기간 동안 발견된 runtime issue를 backlog에 기록한다.

### 자체 테스트

- [x] `uv run pytest` 전체 통과 — API 31 passed, fake-mcp 6 passed
- [x] `pnpm lint` / `pnpm build` 통과
- [x] API smoke: `/health`, `/ready`, `/mcp initialize`, `tools/list`, `tools/call`
- [x] Security smoke: token boundary, redaction, SSRF reject, revoked token 401
- [x] Web route smoke: `/services` 직접 진입, `/toolbox`, `/clients`, `/logs` navigation
- [x] Operations smoke: launchd start, backup, log rotation dry-run
- [ ] docs link smoke: 주요 README/coremcp-docs 링크 유효성 확인

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] `v0.1.0` 후보 상태로 본인 Mac mini에서 운영 가능
- [ ] 문서와 실제 구현의 큰 불일치 없음
- [ ] 모든 필수 테스트 통과
- [ ] 후속 과제는 backlog로 분리됨

---

## 이번 병렬 구현 결과 — 2026-05-11

- Worker A: `apps/api` FastAPI P0 MCP gateway 구현.
- Worker B: `apps/fake-mcp` 테스트용 downstream MCP fixture 구현.
- Worker C: `apps/web` Web Admin UI 초기 scaffold 구현.
- Local integration: root workspace, packages, infra scripts, docs, AGENTS/TESTING 문서 통합.
- 자체 테스트 결과:
  - `make test`: API 13 passed, fake-mcp 6 passed.
  - `pnpm lint`: passed.
  - `pnpm build`: passed.
  - `pnpm test`: passed.
  - actual local smoke: API + fake-mcp uvicorn 실행 후 initialize/tools/list/tools/call/token boundary 통과.

## 잔여 리스크 / 후속 과제

- Keychain은 Mac mini 로그인/잠금 상태에 따라 실패할 수 있다. 장기 무인 운영이면 Fernet mode를 우선 검증한다.
- SQLite WAL/backup 복원은 P3 전까지 운영 리스크가 남는다.
- ChatGPT/Cursor OAuth compatibility는 실제 client 정책 변화가 잦으므로 Optional Phase에서 재검증한다.
- SVG icon은 sanitize 실수보다 default off가 안전하다. 사용 필요가 생기기 전까지 `ICON_SVG_ENABLED=false` 유지.
- Tailscale 외부 접근은 bearer token brute force 방어와 Origin/CORS가 모두 있어도 노출면이 늘어난다. P1 안정화 전에는 localhost만 사용한다.

## 최종 결과 요약

- [x] 병렬 에이전트 기반 최초 scaffold/P0 구현 완료. P1 registry/client token/vault/SSRF는 후속 과제로 남음.
