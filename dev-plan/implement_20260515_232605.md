# implement_20260515_232605.md

작성 일시: `2026-05-15 23:26:05 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
- 마지막으로 큰 MCP handler인 `_handle_tools_call` 본체를 `coremcp.mcp` 모듈로 분리한다.
- repository.py 도메인 분해 전, MCP request handling 응집도를 먼저 정리한다.

## 개발 범위
- tools/call handler 본체를 `mcp/tools_handlers.py`로 이동.
- `ToolsHandlerDeps`를 확장해 main.py helper 의존성을 명시한다.
- `main.py`는 얇은 wrapper만 유지한다.
- export/test/docs를 현재 상태와 맞춘다.

## 제외 범위
- repository.py 본체 도메인 분해는 다음 phase로 유지한다.
- tools/call 동작, plugin 정책, rate limit/circuit breaker/idempotency 정책은 변경하지 않는다.
- 신규 plugin, access group, SaaS 기능은 추가하지 않는다.

## 참조 문서
- [AGENTS.md](../AGENTS.md)
- [coremcp-docs/10-test-plan.md](../coremcp-docs/10-test-plan.md)
- [dev-plan/implement_20260515_231405.md](./implement_20260515_231405.md)

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약
- [x] Phase 1 완료
- [x] Phase 2 완료
- [x] Phase 3 완료

## Phase 1. 의존성 정리
### 목표
- tools/call handler가 main.py에서 참조하던 helper 의존성을 명시적으로 분리한다.

### 구현 태스크
- [x] `_handle_tools_call` 의존 helper 목록 확인.
- [x] `ToolsHandlerDeps` 확장 범위 확정.
- [x] plugin/rate-limit/circuit/idempotency 동작 변경 없음 확인.

### 자체 테스트
- [x] 관련 grep/정적 확인.

### 이슈 및 수정
- [x] 발견 이슈 없음.

### 완료 조건
- [x] 분리 범위 확정.
- [x] 다음 Phase 진행 가능.

## Phase 2. tools/call handler 분리
### 목표
- tools/call 본체를 `mcp/tools_handlers.py`로 이동하고 main.py는 wrapper만 남긴다.

### 구현 태스크
- [x] `handle_tools_call` 추가.
- [x] `mcp/__init__.py` export 갱신.
- [x] main.py `_handle_tools_call` wrapper 전환.
- [x] `ToolsHandlerDeps` 인스턴스 갱신.

### 자체 테스트
- [x] 구조 export 테스트 갱신.
- [x] `cd apps/api && uv run pytest tests/test_structure_facades.py tests/test_mcp_gateway.py -q`

### 이슈 및 수정
- [x] 발견 이슈 없음.

### 완료 조건
- [x] 구현 완료.
- [x] 자체 테스트 완료.
- [x] 다음 Phase 진행 가능.

## Phase 3. 검증/문서 마무리
### 목표
- 전체 테스트와 문서 상태를 최신화한다.

### 구현 태스크
- [x] README 안정화 문장 갱신.
- [x] `coremcp-docs/10-test-plan.md` 안정화 문장 갱신.
- [x] 본 dev-plan 체크박스 완료 처리.

### 자체 테스트
- [x] `cd apps/api && uv run pytest -q`
- [x] `make test`
- [x] `cd apps/api && uv run ruff check coremcp tests/test_structure_facades.py`
- [x] `git diff --check`

### 이슈 및 수정
- [x] 발견 이슈 없음.

### 완료 조건
- [x] 구현 완료.
- [x] 자체 테스트 완료.
- [x] 병합 가능 상태.
