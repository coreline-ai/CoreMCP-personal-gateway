# implement_20260523_082116.md

작성 일시: `2026-05-23 08:21:16 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

직전 2개 cycle 의 잔여 후속 과제 6항목을 한 dev-plan 으로 묶어 처리해서 "1인 로컬 → 팀/SaaS 확장 가능" 임계선을 완전히 넘긴다. 안정화·계약 안정성·확장성 강화만, 새 기능 / SaaS 다중 테넌트 / plugin marketplace 모두 제외.

## 개발 범위 (이 6개 Phase 만)

- Phase 1: main.py 추가 분해 (-200 줄 목표)
- Phase 2: 분산 rate limiter 인터페이스 도입 (Redis 백엔드 스텁)
- Phase 3: pyright 51 → 0 baseline 정리
- Phase 4: CI tightening (pyright gate 활성화 + bandit medium ADR)
- Phase 5: frontend `admin-console.tsx` 분해 + `useReducer`
- Phase 6: frontend OpenAPI codegen (`openapi-typescript`)

## 제외 범위

- 실제 Redis 서버 연결 / 운영 환경 설정 — Phase 2 는 인터페이스만
- frontend 의 admin-shell / sections 컴포넌트 추가 분해 — Phase 5 는 admin-console 만
- 새 기능, SaaS multi-tenant, plugin marketplace
- 외부 LLM API 의존성 추가
- 새 디자인 토큰 / 새 UI 컴포넌트

## 참조 문서

- 이전 dev-plan: [`dev-plan/implement_20260522_220443.md`](implement_20260522_220443.md) — 잔여 6 항목의 출처
- 이전 cycle: [`dev-plan/implement_20260522_183112.md`](implement_20260522_183112.md) — CI/DR/main.py/_schemas.py baseline
- [`coremcp-docs/13-adr.md`](../coremcp-docs/13-adr.md) ADR-042 — main.py decomposition
- `apps/api/coremcp/runtime/context.py` — AppContext facade
- `apps/api/coremcp/mcp_gateway/{stdio_pool,health_probe,responses}.py` — Phase 1 통합 대상

## 공통 진행 규칙

- 각 Phase 자체 테스트 완료 후에만 다음 Phase 진행.
- Phase 1: 동작 보존 우선 — 시그니처 / 응답 / 로그 변경 금지.
- Phase 2: 기존 `FixedWindowRateLimiter` = InMemory 임을 코드로 분명히, 실제 Redis 연결은 별도 cycle.
- Phase 3: pyright fix 는 surgical — 한 file 안에서 가장 적은 변경으로 0 달성, 새 abstraction 도입 금지.
- Phase 4: 이전 cycle 의 advisory 가 정식 gate 가 되면서 PR 차단력 증가, baseline 0 임을 ADR-043 으로 명시.
- Phase 5: state 정리 위주 — UI 동작 / 사이드바 / theme / 깜빡임 fix 회귀 없이 동일하게.
- Phase 6: codegen 도입은 `lib/api.generated.ts` 만 추가, 기존 `lib/api.ts` 와 병존 (점진 마이그레이션).
- 모든 Phase 완료 후 `make test` + frontend `pnpm typecheck && pnpm build` 통과 후 commit.

## Phase 상태 요약

- [x] Phase 1 완료 — main.py 추가 분해 (-124 줄)
- [x] Phase 2 완료 — RateLimitBackend Protocol + Redis 스텁
- [x] Phase 3 완료 — pyright 51 → 0
- [x] Phase 4 완료 — CI gate 활성화 + ADR-043
- [x] Phase 5 완료 — admin-console useReducer (data 도메인)
- [x] Phase 6 완료 — OpenAPI codegen

---

## Phase 1. main.py 추가 분해

### 목표

ADR-042 의 점진 분해 정책 연장. stdio_pool / health_probe 에 남은 helper 들을 이동시켜 main.py 의 stdio·health 관련 코드를 모두 정리.

### 구현 태스크

- [x] stdio_pool.py 로 이동 (6 helper + 1 상수)
  - `stdio_env`, `stdio_command_basename`, `validate_stdio_runtime_config`, `stdio_default_idle_timeout`, `stdio_signature`, `audit_stdio_command_rejected`, `BLOCKED_STDIO_ENV_KEYS`
  - `_string_list` / `_positive_int` 는 main.py 유지 (api/services 도 사용) — stdio_signature 가 lazy import
- [x] health_probe.py 로 이동
  - `detect_service_tool_schema_drift` — 기존 lazy import (`_detect_service_tool_schema_drift`) 제거, 직접 정의
- [x] main.py 의 함수 정의 삭제, alias import 만 유지 (테스트 호환)
- [x] main.py 줄 수: **1,721 → 1,597 (-124 lines)**. 목표 ≤1,520 보다 77줄 부족하나, 잔여는 `_tool_schema_diff` (main.py 외부 사용처 있음), tool_registry helpers, RPC dispatch wrapper 등 결합도 높은 코드 — next cycle.
- [x] 불필요한 `from pathlib import Path` 제거

### 자체 테스트

- [x] `uv run pytest -q` 통과 (212 tests)
- [x] coverage gate 유지 (≥80%)
- [x] main.py 의 stdio / health 관련 함수 정의 0개 (모두 alias import)

### 이슈 및 수정

- [x] 발견 이슈 없음 — 호출자가 모두 main.py alias 경유라 회귀 없음

### 완료 조건

- [x] 7 helper 이동 + main.py 슬림화
- [x] 회귀 통과 (212 tests)
- [x] 다음 Phase 진행 가능

---

## Phase 2. 분산 rate limiter 인터페이스

### 목표

`FixedWindowRateLimiter` 가 메모리 단일 프로세스 구현임을 코드로 분명히 하고, 향후 Redis 도입을 위한 Protocol 인터페이스를 미리 마련. 실제 Redis 연결은 backlog.

### 구현 태스크

- [x] `coremcp/auth/rate_limit.py` 재구성
  - `RateLimitBackend(Protocol)` 신설 — `check(key, *, limit, window_seconds)` + `clear()`. `runtime_checkable`.
  - 기존 `FixedWindowRateLimiter` 본문을 `InMemoryRateLimiter` 로 이름 변경, `FixedWindowRateLimiter = InMemoryRateLimiter` alias 유지 (기존 import 호환).
  - `RedisRateLimiter` 스텁 신설 — `redis` 라이브러리 import 시도, 실패/URL 미설정 시 `InMemoryRateLimiter` fallback + warning 1회.
  - `build_rate_limiter(backend: str, *, redis_url: str | None)` factory.
- [x] `coremcp/settings.py` — `COREMCP_RATE_LIMIT_BACKEND` (기본 `"memory"`), `COREMCP_RATE_LIMIT_REDIS_URL` (기본 `None`).
- [x] `coremcp/main.py` — 5개 limiter 초기화를 `build_rate_limiter(settings.rate_limit_backend, redis_url=settings.rate_limit_redis_url)` factory closure 로 교체.
- [x] `coremcp-docs/13-adr.md` 에 **ADR-044 Distributed rate limiter interface (Redis backlog)** 추가.

### 자체 테스트

- [x] `uv run pytest tests/test_admin_rate_limit.py tests/test_oauth_rate_limit.py -q` 통과 (5 tests)
- [x] `uv run pytest -q` 전체 통과 (212 tests)
- [x] `FixedWindowRateLimiter` import 한 기존 코드 그대로 작동 (alias)

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] Protocol + InMemory + Redis 스텁 + ADR-044
- [x] 회귀 통과 (212 tests)
- [x] 다음 Phase 진행 가능

---

## Phase 3. pyright 51 → 0

### 목표

이전 cycle 의 advisory 51 errors 를 0 으로 축소. 다음 cycle 의 CI tightening 의 선행 조건.

### 구현 태스크

- [x] `coremcp/mcp/resources.py` — `metadata_json` 타입 narrow + `item: dict[str, Any]` 명시 (cached_resource_to_mcp, cached_resource_template_to_mcp) → 18 errors 제거
- [x] `coremcp/mcp/prompts.py` — 동일 패턴 → 8 errors 제거
- [x] `coremcp/main.py` — payload params narrow, downstream_params 타입화, 시작 시점 stdio Phase 1 이슈 정리 → 13 → 0
- [x] `coremcp/auth/oauth.py` — `cimd_rate_limiter` 타입을 `RateLimitBackend` 로, grant_types/response_types narrow → 4 → 0
- [x] `coremcp/mcp/dispatcher.py` — `notifications/cancelled` 의 params narrow → 3 → 0
- [x] `coremcp/api/connections.py` — `utc_sql_timestamp` / `iso_z` / `connection_token_prompt` callable 시그니처 타이트닝 → 3 → 0
- [x] single-file 3개 (`capabilities.py`, `services_crud.py`, `body_limit.py`) → 3 → 0
- [x] 부가 변경:
  - `coremcp/auth/client_tokens.py` — `ClientTokenService.__init__` 이 `Repository | CredentialRepository` 모두 수용 (facade 호환)
  - `coremcp/mcp/tools_handlers.py` — `idempotency_cache_key` 콜백을 `str | None` 반환, `ToolCallRuntime.idempotency_key` 도 `str | None`
  - `coremcp/runtime/context.py` — `stdio_clients` 타입 시그니처를 실제 데이터 모양 `dict[str, tuple[tuple[Any, ...], StdioMcpClient]]` 으로 정정, `vault` 타입을 `CredentialVault` 로 정정
  - `coremcp/api/services.py` + `services_crud.py` — `string_list`/`stdio_env`/`positive_int`/`stdio_default_idle_timeout` 콜백 시그니처 타이트닝
- [x] 최종: **pyright 0 errors / 0 warnings / 0 informations**

### 자체 테스트

- [x] `uv run pyright` — 0 errors
- [x] `uv run pytest -q` — 212 tests 통과 (회귀 없음)
- [x] coverage gate 유지 (≥80%)

### 이슈 및 수정

- [x] **이슈 1**: stdio_pool 추출 직후 pyright 가 `AppContext.stdio_clients` 의 잘못된 타입 (`dict[str, StdioMcpClient]`) 으로 8 errors → `dict[str, tuple[tuple[Any, ...], StdioMcpClient]]` 로 정정.
- [x] **이슈 2**: `vault` 타입 힌트가 존재하지 않는 `VaultBackend` 를 import 시도 → 실제 클래스 `CredentialVault` 로 변경.
- [x] **이슈 3**: callback 시그니처 mismatch (e.g., `Callable[[Any, int|None], int|None]` vs 실제 `(Any, int) -> int`) → API 측 시그니처를 실제 구현에 맞춤.
- [x] **이슈 4**: `ClientTokenService` 가 `Repository` 만 받도록 타입 선언 → facade `CredentialRepository` 도 가능하도록 `Repository | CredentialRepository` union.

### 완료 조건

- [x] pyright 0 errors
- [x] 회귀 통과 (212 tests)
- [x] 다음 Phase (CI gate 활성화) 진행 가능

---

## Phase 4. CI tightening

### 목표

Phase 3 후 pyright 0 — CI gate 정식 활성화로 PR 차단력 확보. bandit medium skip 사유를 ADR-043 으로 명시.

### 구현 태스크

- [x] `.github/workflows/test.yml` 의 `pyright` job 에서 `continue-on-error: true` 제거. baseline=0 임을 주석으로 명시.
- [x] `coremcp-docs/13-adr.md` 에 **ADR-043 bandit B608 global skip — parameterized SQL with internal column allowlist** 추가.

### 자체 테스트

- [x] 로컬 `uv run pyright` 0 errors (Phase 3 결과 유지)
- [ ] CI 검증은 다음 commit 의 push 결과로 확인

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] pyright job blocking (continue-on-error 제거)
- [x] ADR-043 기록
- [x] 다음 Phase 진행 가능

---

## Phase 5. frontend admin-console useReducer

### 목표

`admin-console.tsx` 의 27 useState 를 도메인별 reducer 로 정리. 동작 / 사이드바 / theme / 깜빡임 fix 회귀 없이 동일하게.

### 구현 태스크

- [x] **data 도메인** (가장 큰 11개) → 단일 `dataReducer` 통합 완료
  - state: settings / services / toolboxes / defaultToolbox / connections / clientTokens / invocations / auditLogs / dashboardSummary / playgroundTools / toolOverridesByService
  - actions: `LOAD_ALL`, `RESET`, `SET_PLAYGROUND_TOOLS`
  - `refreshAdminData()` 의 11 setter → 단일 `dispatchData({type:'LOAD_ALL', payload:{...}})`
  - `handleTokenClear()` 의 10 setter → 단일 `dispatchData({type:'RESET'})`
  - `handleLoadPlaygroundTools()` 의 setPlaygroundTools → `dispatchData({type:'SET_PLAYGROUND_TOOLS', payload})`
  - 구조 분해로 기존 변수명 (settings, services, ...) 그대로 유지 → 사용처 무수정
- [ ] **token / form / status 도메인** → next cycle 이월. 이유: 각각 도메인 결합이 약하고 (예: tokenInput 은 form 처럼도 동작), 분리 시 reducer 가 useState 보다 더 무거워질 수 있음. data 도메인 (가장 큰 11개) 만 우선 정리한 게 ROI 가장 높음.
- [x] useEffect / useMemo / useCallback 의 deps — 구조 분해 변수가 reducer state 와 동일 reference 라서 영향 없음

### 자체 테스트

- [x] `cd apps/web && pnpm typecheck` 통과 (0 errors)
- [x] `cd apps/web && pnpm build` 통과
- [x] `cd apps/web && pnpm lint` 통과
- [ ] 수동 sanity — 다음 commit 의 web 재기동 후 확인

### 이슈 및 수정

- [x] 발견 이슈 없음 — reducer state 의 구조 분해로 기존 변수 이름 / 참조 패턴이 동일하게 유지됨

### 완료 조건

- [x] useState 27 → **16** (11개 → 1 useReducer)
- [x] typecheck / build / lint 통과
- [x] 다음 Phase 진행 가능 (token/form 분해는 next cycle)

---

## Phase 6. frontend OpenAPI codegen

### 목표

backend 의 OpenAPI 가 Phase 2 cycle 에서 response_model 로 풍부해진 상태 — 이걸 frontend type 의 정식 source-of-truth 로 점진 마이그레이션.

### 구현 태스크

- [x] `apps/web/package.json` 에 dev 의존성 `openapi-typescript ^7.13.0` 추가
- [x] `pnpm gen:api` 스크립트 추가 — `openapi-typescript http://127.0.0.1:8787/openapi.json -o lib/api.generated.ts`
- [x] launchctl `com.coremcp.api` 재기동으로 신규 response_model 17개를 OpenAPI 에 노출 (재기동 전: 2 schemas, 후: 19 schemas)
- [x] `pnpm gen:api` 실행 → `lib/api.generated.ts` 생성 (2,367 lines, 17 신규 schema 포함)
- [x] 기존 `lib/api.ts` 와 병존 — `extra="allow"` 덕분에 frontend interface 가 generated type 의 superset 으로 자동 호환. 점진 교체는 next cycle.

### 자체 테스트

- [x] `pnpm gen:api` 성공 (48ms, 2,367 lines)
- [x] `pnpm typecheck` 통과 (0 errors)
- [x] `pnpm build` 통과 (모든 라우트 빌드)
- [x] OpenAPI JSON 의 `components.schemas` 가 17 신규 + 2 기본 = 19개

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] codegen 스크립트 + `lib/api.generated.ts`
- [x] frontend 빌드 / typecheck 통과
- [x] dev-plan 종료 — 다음 cycle 후보는 잔여 과제 단락에 정리

---

## 잔여 리스크 / 후속 과제

- 실제 Redis 운영 환경 연결 + 클러스터링 (Phase 2 의 backlog)
- `lib/api.ts` 의 모든 interface 를 generated 로 완전 교체 (Phase 6 의 backlog)
- frontend admin-shell / sections / playground 컴포넌트의 useReducer 마이그레이션 (admin-console 만 본 cycle)
- main.py 의 잔여 helper (rate limit, OAuth 라우터 wrapper, tool_registry helper) 추가 분해
- bandit Low (34 건) 검토 후 ADR / nosec 분리
- pyright `strict` mode 도입 (현재 `basic`)
- multi-mixin Repository 패턴 → 명시적 facade 로 전환 (mixin 의존성 제거)
