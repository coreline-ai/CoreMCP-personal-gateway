# implement_20260515_174812.md

작성 일시: `2026-05-15 17:48:12 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

멀티-MCP gateway의 P0 blocker 이후 남은 P1 정합성/운영성 고도화 항목을 구현한다.

## 개발 범위

- Dynamic capability merge
- Tool args JSON Schema 사전 검증
- Per-service rate limit / quota
- `tools/list` unavailable service metadata
- Health probe schema drift detection
- Idempotency-Key downstream forward

## 제외 범위

- SaaS/team/workspace/marketplace/billing
- sampling/roots/elicitation 양방향 RPC 구현
- Service credential 자동 refresh
- main.py / repository.py 대규모 모듈 분해
- 외부환경 검증(reboot/Tailscale/real OAuth/mobile/long soak)

## 참조 문서

- [AGENTS.md](../AGENTS.md)
- [coremcp-docs/07-mcp-proxy-spec.md](../coremcp-docs/07-mcp-proxy-spec.md)
- [coremcp-docs/10-test-plan.md](../coremcp-docs/10-test-plan.md)
- [dev-plan/implement_20260515_061719.md](./implement_20260515_061719.md)

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약

- [x] Phase 1 완료
- [x] Phase 2 완료
- [x] Phase 3 완료
- [x] Phase 4 완료
- [x] Phase 5 완료

## Phase 1. Capability merge

### 목표

- service validation에서 downstream initialize capabilities를 저장하고, CoreMCP initialize 응답에서 active service capabilities union을 반환한다.

### 구현 태스크

- [x] `mcp_services.capabilities_json` migration 추가
- [x] Repository read/write json field 처리
- [x] `validate_service()`에서 init result capabilities 저장
- [x] `SERVER_CAPABILITIES` 고정 응답을 dynamic builder로 대체

### 자체 테스트

- [x] resources/prompts 없는 service만 active일 때 capabilities에서 resources/prompts 생략
- [x] resources/prompts 있는 service validation 후 capabilities 포함

## Phase 2. Tool args JSON Schema validation

### 목표

- cached `input_schema_json`으로 `tools/call` arguments를 gateway에서 사전 검증한다.

### 구현 태스크

- [x] `jsonschema` 의존성 추가
- [x] validation helper 추가
- [x] 실패 시 downstream 호출 없이 JSON-RPC `-32602`와 `policy.invalid_args` audit 기록

### 자체 테스트

- [x] required field 누락 시 downstream 호출 없음
- [x] 정상 args는 기존 success path 유지

## Phase 3. Per-service rate limit + Idempotency-Key forwarding

### 목표

- 특정 service 폭주를 in-memory fixed-window로 제한하고, downstream 자체 idempotency를 쓸 수 있게 safe header를 전달한다.

### 구현 태스크

- [x] settings 추가: `COREMCP_SERVICE_RATE_LIMIT_PER_MINUTE`
- [x] app state limiter 추가
- [x] `tools/call` service route 기준 limiter 적용
- [x] HTTP downstream request에 `Idempotency-Key` safe header 전달

### 자체 테스트

- [x] limit 초과 시 downstream 호출 차단
- [x] Idempotency-Key가 downstream header로 전달됨

## Phase 4. Unavailable metadata + health drift

### 목표

- `tools/list`에 circuit-open/unhealthy service metadata를 노출하고, health probe가 schema drift를 감지해 catalog를 갱신한다.

### 구현 태스크

- [x] catalog tools/list 결과 `_meta.coremcp.unavailable_services` 추가
- [x] health probe success path에서 `tools/list` hash compare
- [x] drift 발견 시 validation/catalog refresh + listChanged publish

### 자체 테스트

- [x] circuit-open service가 metadata에 표시됨
- [x] health probe tools/list 변화가 service validation summary/listChanged로 반영됨

## Phase 5. 검증/문서

### 자체 테스트

- [x] targeted pytest
- [x] API full pytest
- [x] `make test`
- [x] `git diff --check`
- [x] README/TESTING/coremcp-docs 업데이트

### 진행 기록

- `cd apps/api && uv run pytest tests/test_bootstrap.py ... -q` — 10 passed
- `cd apps/api && uv run pytest tests/test_mcp_gateway.py -q` — 79 passed
- `cd apps/api && uv run pytest -q` — 140 passed
- `make test` — API 140 + fake-mcp 12 + demo-mcp-suite 21 passed
- `git diff --check` — PASS
