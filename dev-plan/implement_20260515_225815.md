# implement_20260515_225815.md

작성 일시: `2026-05-15 22:58:15 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
- 코드 레벨 안정화 리뷰에서 확인된 작은 lifecycle/exception 일관성 이슈를 보강한다.
- 새 기능 추가 없이 현재 구조의 종료 처리, 오류 계층, 테스트 계약을 안정화한다.

## 개발 범위
- `OAuthService.shutdown()` 추가 및 FastAPI lifespan teardown 연결.
- CoreMCP 공통 예외 base class 도입 및 기존 CoreMCP 예외 계층 연결.
- 현재 코드 상태 기준으로 이미 해결된 항목과 의도적으로 제외하는 항목을 문서화.
- 자체 테스트 및 관련 문서 카운트 업데이트.

## 제외 범위
- Plugin hook 범위 확장(resources/read, prompts/get)은 보안 ADR 선행 전까지 제외한다.
- RepositoryFacade 전체 call-site migration은 별도 구조 안정화 phase로 유지한다.
- 새로운 built-in plugin, 외부 plugin loading, access group, SaaS 기능은 추가하지 않는다.
- FastAPI router 추가 분리 및 repository.py 본체 분해는 이번 범위에서 제외한다.

## 참조 문서
- [AGENTS.md](../AGENTS.md)
- [CLAUDE.md](../CLAUDE.md)
- [TESTING.md](../TESTING.md)
- [coremcp-docs/10-test-plan.md](../coremcp-docs/10-test-plan.md)
- [dev-plan/implement_20260515_225153.md](./implement_20260515_225153.md)

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약
- [x] Phase 1 완료
- [x] Phase 2 완료
- [x] Phase 3 완료

## Phase 1. 현재 코드 상태 재검증
### 목표
- 리뷰 항목 중 이미 해결된 항목과 실제 패치가 필요한 항목을 분리한다.

### 구현 태스크
- [x] OAuth shutdown 부재 확인.
- [x] downstream session invalidation hook이 이미 route/circuit-breaker에 연결됐는지 확인.
- [x] RepositoryFacades가 route module에서 실제 사용 중인지 확인.
- [x] Plugin hook 확장은 보안 ADR 선행 후속으로 유지.

### 자체 테스트
- [x] 관련 코드 grep/정적 확인.

### 이슈 및 수정
- [x] 발견 이슈 없음 — 실제 패치 필요 항목은 OAuth shutdown + CoreMCP 공통 예외 base로 한정됨.

### 완료 조건
- [x] 구현 필요 항목이 OAuth shutdown + exception base로 좁혀짐.
- [x] 다음 Phase 진행 가능.

## Phase 2. Lifecycle / exception 안정화
### 목표
- OAuth service 종료 계약과 CoreMCP 예외 계층을 일관화한다.

### 구현 태스크
- [x] `coremcp/errors.py` 공통 예외 base 추가.
- [x] 기존 CoreMCP 예외가 `CoreMcpError` 계층을 상속하도록 변경.
- [x] `OAuthService.shutdown()` 추가.
- [x] lifespan teardown에서 OAuth shutdown 호출.

### 자체 테스트
- [x] exception 계층 회귀 테스트 추가.
- [x] OAuth shutdown 계약 회귀 테스트 추가.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료.
- [x] 자체 테스트 완료.
- [x] 다음 Phase 진행 가능.

## Phase 3. 문서/검증 마무리
### 목표
- 테스트 카운트와 안정화 상태 문서를 현재 코드와 맞춘다.

### 구현 태스크
- [x] `TESTING.md` 테스트 카운트 업데이트.
- [x] `coremcp-docs/10-test-plan.md` 테스트 카운트 업데이트.
- [x] 본 dev-plan 체크박스 완료 처리.

### 자체 테스트
- [x] `cd apps/api && uv run pytest -q`
- [x] `make test`
- [x] `git diff --check`

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료.
- [x] 자체 테스트 완료.
- [x] 병합 가능 상태.
