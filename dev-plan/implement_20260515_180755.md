# implement_20260515_180755.md

작성 일시: `2026-05-15 18:07:55 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

수정 후 멀티-MCP 리뷰에서 남은 것으로 분류된 안정화 항목을 현재 코드 기준으로 재검증하고, 실제 미흡한 부분만 패치한다.

## 개발 범위

- downstream session lifecycle 보강: TTL, lazy/reaper expiration, circuit-open invalidation
- Resource URI shadow 정책: cross-service collision 시 가장 최근 catalog row만 active 유지
- notification callback service context 점검 및 문서화
- JSON-RPC batch 정책 문서화

## 제외 범위

- main.py/repository.py 대규모 모듈 분해
- bidirectional RPC(sampling/roots/elicitation)
- SaaS/team/workspace/marketplace/billing
- 실제 외부환경 검증(reboot/Tailscale/real OAuth/mobile/long soak)

## 참조 문서

- [AGENTS.md](../AGENTS.md)
- [dev-plan/implement_20260515_061719.md](./implement_20260515_061719.md)
- [dev-plan/implement_20260515_174812.md](./implement_20260515_174812.md)

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약

- [x] Phase 1 완료
- [x] Phase 2 완료
- [x] Phase 3 완료
- [x] Phase 4 완료

## Phase 1. Downstream session lifecycle

### 목표

- downstream session id cache가 영구 고착되지 않도록 TTL과 failure invalidation을 추가한다.

### 구현 태스크

- [x] `COREMCP_DOWNSTREAM_SESSION_TTL_SECONDS` 설정 추가
- [x] session cache payload를 `{session_id, expires_at, updated_at}` 형태로 저장
- [x] lookup 시 만료 session lazy cleanup
- [x] background reaper tick에서 expired downstream session cleanup
- [x] circuit breaker가 open 상태가 되면 해당 service downstream session invalidate

### 자체 테스트

- [x] TTL 만료 후 후속 request에 stale `Mcp-Session-Id`가 전달되지 않음
- [x] circuit-open failure 후 downstream session이 제거됨

## Phase 2. Resource URI shadow policy

### 목표

- cross-service duplicate resource URI를 비결정적 first-hit/ambiguous hide가 아니라 명시적 shadow 정책으로 정리한다.

### 구현 태스크

- [x] resource catalog refresh 후 duplicate URI 중 최신 row만 `active`
- [x] 나머지는 `deprecated` + audit `resource.shadow`
- [x] `resources/list/read`는 active winner만 노출/라우팅

### 자체 테스트

- [x] duplicate URI 두 service validation 시 하나만 active 노출
- [x] shadow audit 기록

## Phase 3. 리뷰 항목 재검증 문서화

### 목표

- 이미 구현된 `unavailable_services`, health drift, STDIO LRU, batch sequential 정책을 문서에 반영한다.

### 구현 태스크

- [x] `coremcp-docs/07-mcp-proxy-spec.md` 업데이트
- [x] `coremcp-docs/10-test-plan.md` 업데이트
- [x] `TESTING.md` 업데이트

### 자체 테스트

- [ ] 문서 내 stale test count/old migration 없음

## Phase 4. 검증

### 자체 테스트

- [x] targeted pytest
- [x] API full pytest
- [x] make test
- [x] git diff --check

## 진행 기록

- `cd apps/api && uv run pytest tests/test_mcp_gateway.py::test_duplicate_resource_uri_uses_shadow_policy tests/test_mcp_gateway.py::test_downstream_session_ttl_expires_before_forwarding tests/test_mcp_gateway.py::test_circuit_open_invalidates_downstream_session -q` — 3 passed
- `cd apps/api && uv run pytest -q` — 142 passed
- `make test` — API 142 + fake-mcp 12 + demo-mcp-suite 21 passed
- `git diff --check` — PASS
