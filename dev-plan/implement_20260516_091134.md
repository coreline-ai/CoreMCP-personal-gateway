# implement_20260516_091134.md

작성 일시: `2026-05-16 09:11:34 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
- 확장 개발 없이, 코드 리뷰에서 확인된 안정성 보강 항목만 처리한다.
- downstream session stale 가능성과 plugin 예외 전파 가능성을 줄인다.

## 개발 범위
- credential/auth 변경 시 downstream session invalidation을 명시적으로 수행한다.
- Plugin hook 예외를 공통 예외로 감싸고 tools/call에서 fail-closed tool error로 반환한다.
- Plugin 예외 정책을 ADR에 문서화한다.
- 회귀 테스트를 추가한다.

## 제외 범위
- built-in plugin 추가, 외부 plugin loading, access group, SaaS 기능은 제외한다.
- repository.py 본체 도메인 분해는 다음 phase로 유지한다.
- validate_service/health probe RPC helper 통일은 이번 범위에서 제외한다.
- Plugin hook 범위를 resources/prompts로 확장하지 않는다.

## 참조 문서
- [AGENTS.md](../AGENTS.md)
- [coremcp-docs/13-adr.md](../coremcp-docs/13-adr.md)
- [coremcp-docs/10-test-plan.md](../coremcp-docs/10-test-plan.md)

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약
- [x] Phase 1 완료
- [x] Phase 2 완료
- [x] Phase 3 완료

## Phase 1. Downstream session invalidation 보강
### 목표
- credential/auth 변경 뒤 기존 downstream session이 stale 상태로 남지 않도록 한다.

### 구현 태스크
- [x] service `auth_type` 변경 시 downstream session 제거.
- [x] credential put/rotate/delete 시 downstream session 제거.

### 자체 테스트
- [x] credential 변경 session invalidation 회귀 테스트 추가.

### 이슈 및 수정
- [x] 발견 이슈 없음.

### 완료 조건
- [x] 구현 완료.
- [x] 자체 테스트 완료.
- [x] 다음 Phase 진행 가능.

## Phase 2. Plugin 예외 정책/guard
### 목표
- plugin hook 예외가 unhandled 500으로 전파되지 않도록 fail-closed로 고정한다.

### 구현 태스크
- [x] `PluginExecutionError` 추가.
- [x] registry가 plugin 이름/stage를 보존해 예외를 감싼다.
- [x] tools/call before/after hook 실패 시 raw args/result 없이 audit + invocation 기록.
- [x] client에는 tool-level `isError=true` 응답을 반환한다.
- [x] ADR에 policy 기록.

### 자체 테스트
- [x] registry 예외 wrapping 테스트 추가.
- [x] tools/call plugin 실패 통합 테스트 추가.

### 이슈 및 수정
- [x] 발견 이슈 없음.

### 완료 조건
- [x] 구현 완료.
- [x] 자체 테스트 완료.
- [x] 다음 Phase 진행 가능.

## Phase 3. 검증/문서 마무리
### 목표
- 전체 테스트와 문서 상태를 최신화한다.

### 구현 태스크
- [x] `README.md` / `TESTING.md` / `10-test-plan.md` 테스트 카운트 갱신.
- [x] 본 dev-plan 체크박스 완료 처리.

### 자체 테스트
- [x] `cd apps/api && uv run pytest -q`
- [x] `make test`
- [x] `cd apps/api && uv run ruff check coremcp tests/test_mcp_gateway.py tests/test_plugins.py`
- [x] `git diff --check`

### 이슈 및 수정
- [x] 발견 이슈 없음.

### 완료 조건
- [x] 구현 완료.
- [x] 자체 테스트 완료.
- [x] 병합 가능 상태.
