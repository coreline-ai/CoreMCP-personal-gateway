# CoreMCP P0/P1 재검증 패치 계획 — 2026-05-13 22:17

## 목표

사용자 제공 재분석 보고서를 현재 코드 기준으로 다시 실측하고, 실제로 남은 보안/운영 회귀 위험만 최소 diff로 보강한다.

## 실측 결과 요약

- P0-A Fernet: 구현/테스트 존재.
- P0-B CSP: `script-src 'unsafe-inline'`은 현재 없음. 추가 하드닝으로 `style-src 'unsafe-inline'`도 제거한다.
- P0-C daily backup launchd: 구현 존재.
- P0-D Web route smoke: Playwright CLI 기반 smoke 존재. CSP header 검증을 smoke에 포함한다.
- P0-E CIMD charset: `Application/JSON; Charset=UTF-8` 통과 테스트 존재.
- B1 SSE listChanged: process-local event bus + toolbox/service 변경 publish 구현 존재.
- B3 refresh token rotation: family reuse detection + cascade revoke 테스트 존재.
- F1/F2/F3: route split, service detail, sessionStorage 전환 구현 존재.
- fake-mcp fixtures: cancellation/schema-change/cimd/dcr/icons-rich 구현 존재.
- ALLOW_TAILSCALE_DOWNSTREAM: 설정/SSRF checker/test 구현 존재.

## 구현 작업

- [x] 현재 코드와 테스트로 보고서 항목 재검증
- [x] Downstream timeout을 tool-level timeout 결과 + invocation `timeout`으로 명시 매핑
- [x] CSP `style-src 'unsafe-inline'` 제거
- [x] Web route smoke에 CSP/security header 회귀 검증 추가
- [x] SVG icon default block 회귀 테스트 추가
- [x] 문서/테스트 계획 상태 업데이트
- [x] API/fake/web lint/build/smoke 검증

## 검증 결과

- `make test`: PASS — API 46 passed, fake-mcp 12 passed.
- `pnpm lint`: PASS.
- `pnpm build`: PASS.
- `pnpm test`: PASS.
- `make smoke`: PASS.
- `COREMCP_WEB_URL=http://127.0.0.1:3004 infra/scripts/web-route-smoke.sh`: PASS.
- `git diff --check`: PASS.

## 비범위

- SaaS team/workspace/marketplace/billing 기능
- 외부 LLM API 의존성 추가
- 실제 Tailscale 설치/login/Serve와 macOS reboot 검증 자동 수행
