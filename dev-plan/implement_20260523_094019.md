# implement_20260523_094019.md

작성 일시: `2026-05-23 09:40:19 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

직전 3 cycle 의 잔여 후속 5 항목 중 코드로 처리 가능한 4 항목을 한 dev-plan 으로 마무리한다. 운영 Redis 환경 연결은 인프라 결정이 필요해 본 cycle 에서 제외. 신기능 없음, baseline 0 유지.

## 개발 범위 (이 4 Phase)

- Phase 1: frontend `logs / clients / playground` section useReducer 마이그레이션
- Phase 2: `lib/api.ts` 잔여 interface (ServiceTool / ToolOverride / ToolboxItem / ToolPreset / ServiceCredential 등) generated 로 완전 전환
- Phase 3: ADR-046 Step 1 — `JobsRepositoryMixin` → `JobsRepository` facade 전환
- Phase 4: pyright `strict` mode 시도

## 제외 범위

- 운영 Redis 환경 연결 (URL/auth/Tailscale ACL) — 별도 cycle
- ADR-046 의 Step 2/3 (Audit / 나머지 5 mixin) — Step 1 안정화 후 별도 cycle
- 새 기능, SaaS multi-tenant, plugin marketplace
- 외부 LLM API 의존성 추가

## 참조 문서

- 이전 dev-plan: [`dev-plan/implement_20260523_092359.md`](implement_20260523_092359.md) — 잔여 후속 출처
- [`coremcp-docs/13-adr.md`](../coremcp-docs/13-adr.md) — ADR-042~046
- `apps/api/coremcp/db/repository.py`, `apps/api/coremcp/db/repository_jobs.py`
- `apps/web/lib/api.ts`, `apps/web/lib/api.generated.ts`
- `apps/web/components/admin/sections/{logs,clients,playground}-section.tsx`

## 공통 진행 규칙

- 각 Phase 자체 테스트 완료 후에만 다음 Phase 진행.
- 동작 보존 우선 — 시그니처 / 응답 / 로그 변경 금지.
- Phase 3 의 facade 전환은 1 mixin (Jobs) 만, 트랜잭션 boundary 변경 금지.
- Phase 4 의 strict mode 가 50+ warning 을 만들면 surgical fix 후에도 standard 로 revert + ADR 기록.
- 모든 Phase 완료 후 `make test` + `pnpm typecheck && pnpm build && pnpm lint` 통과 후 commit.

## Phase 상태 요약

- [x] Phase 1 완료 — clients / playground useReducer (logs 는 1 state 라 skip)
- [x] Phase 2 완료 — lib/api.ts 잔여 generated 전환 (4 중 2 alias 적용, 2 는 narrowing 차이로 manual 유지)
- [x] Phase 3 완료 — Jobs mixin → TYPE_CHECKING 명시 (ADR-046 Step 1)
- [x] Phase 4 완료 — pyright standard baseline 보존 (strict 943 errors → ADR-047)

---

## Phase 1. logs / clients / playground useReducer

### 목표

각 section 의 다중 useState 를 도메인별 reducer 로 통합. 동작 보존, UI 영향 0.

### 구현 태스크

- [ ] `logs-section.tsx` (2 useState) — `logsReducer`
- [ ] `clients-section.tsx` (3 useState) — `clientsReducer`
- [ ] `playground-section.tsx` (4 useState) — `playgroundReducer`
- [ ] 각 reducer 의 actions 명확히 (SET_*, RESET 등)
- [ ] 기존 setter 호출 → dispatch

### 자체 테스트

- [ ] `pnpm typecheck` 통과
- [ ] `pnpm build` 통과
- [ ] `pnpm lint` 통과
- [ ] 수동 sanity — 다음 commit 의 web 재기동 후

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 3 section useReducer 적용
- [ ] 빌드 통과
- [ ] 다음 Phase 진행 가능

---

## Phase 2. lib/api.ts 잔여 interface generated 전환

### 목표

직전 cycle Phase 4 의 6 도메인 alias 외에 잔여 interface 도 generated 로 정렬. 프론트 응답 shape 의 single-source-of-truth 를 backend OpenAPI 로 통일.

### 구현 태스크

- [ ] 잔여 interface 인벤토리: `ServiceToolSummary`, `ToolOverrideSummary`, `ToolPresetResponse`, `ServiceCredentialSummary`, `ToolboxItemSummary`, `IssueClientTokenResponse`, `OneTimeConnectionTokenResponse`, `ToolInvocationSummary`, `AuditLogSummary`, `DashboardSummary`, `SettingsResponse`, `CodexSimulatorToolCall`, `HealthResponse`, `ListResponse`
- [ ] generated `components["schemas"]` 에 있는 schema 와 1:1 매핑: 직접 alias 또는 extends
- [ ] generated 에 없는 schema (Dashboard / Settings / Audit / Invocation 등 backend 가 response_model 안 붙은 라우트) 는 그대로 유지 + 주석으로 "next cycle 에 backend response_model 추가" 명시
- [ ] `pnpm typecheck` 회귀 0

### 자체 테스트

- [ ] `pnpm typecheck` 통과
- [ ] `pnpm build` 통과
- [ ] `pnpm lint` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 잔여 interface alias 적용 + 주석
- [ ] 빌드 통과
- [ ] 다음 Phase 진행 가능

---

## Phase 3. Jobs mixin → JobsRepository facade

### 목표

ADR-046 의 Step 1. 결합도 가장 낮은 `JobsRepositoryMixin` 을 명시 facade 로 전환 + `repository_jobs.py` 파일의 `# pyright: reportAttributeAccessIssue=false` 디렉티브 제거.

### 구현 태스크

- [ ] `repository_jobs.py` 의 `JobsRepositoryMixin` 클래스를 `JobsRepository(repository: Repository)` 명시 클래스로 전환
  - `self.db`, `self.dumps_json`, `self._row_to_dict` 를 명시 의존으로 받음 (생성자에서 `Repository` 보관)
  - 메서드 시그니처 / 트랜잭션 boundary / 응답 dict shape 동일
- [ ] `Repository.__init__` 에 `self.jobs: JobsRepository = JobsRepository(self)` 추가
- [ ] 기존 mixin 호출 (`repository.create_job`, `repository.complete_job` 등) 을 호출 site 가 직접 사용한다면 `repository.jobs.create_job` 으로 점진 교체 — 단 facade 전환 cycle 의 안전성을 위해 backward-compat alias 메서드 유지
- [ ] 파일 상단 `# pyright: reportAttributeAccessIssue=false` 디렉티브 제거 가능 여부 확인 후 제거

### 자체 테스트

- [ ] `make test-api` 통과 (212 + Redis 7 = 219)
- [ ] `uv run pyright` 0 errors 유지
- [ ] coverage gate 유지

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] Jobs facade 적용 + 디렉티브 제거 (가능 시)
- [ ] 회귀 + pyright 0
- [ ] 다음 Phase 진행 가능

---

## Phase 4. pyright `strict` mode

### 목표

`typeCheckingMode = "standard"` → `"strict"`. 발생하는 warning 을 surgical fix or `standard` baseline 보존 결정.

### 구현 태스크

- [ ] `pyproject.toml` 의 `typeCheckingMode = "standard"` → `"strict"`
- [ ] `uv run pyright` 실행 → warning 카운트
- [ ] 50건 미만이면 surgical fix
- [ ] 50건 이상이면 `standard` 로 revert + ADR-047 "pyright strict mode 도입 전 처리해야 할 사항" 으로 기록

### 자체 테스트

- [ ] `uv run pyright` 결과 errors 0 + (warnings ≤ 50 또는 strict revert 사유 명확)
- [ ] `uv run pytest -q` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] strict mode 적용 OR baseline 보존 + ADR-047
- [ ] dev-plan 종료

---

## 잔여 리스크 / 후속 과제

- 운영 Redis 환경 연결 (URL/auth/Tailscale ACL) — 인프라 결정 후 진행
- ADR-046 의 Step 2/3 — Jobs 안정화 후 Audit / 나머지 5 mixin
- backend 의 Dashboard / Settings / Audit / Invocation 라우트 response_model 추가 (Phase 2 의 next-cycle 후보)
- frontend `dashboard-section / services-section / settings-section / toolbox-section` 의 useReducer (남은 sections)
- pyright `strict` mode 의 50+ warning fix (Phase 4 결과 baseline 보존 시)
