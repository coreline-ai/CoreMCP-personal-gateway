# implement_20260515_221252.md

작성 일시: `2026-05-15 22:12:52 KST`

이 문서는 남은 안정화 후보 중 route 분리 위험이 낮은 settings/dashboard/audit/jobs route 분리 범위를 고정한다.

## 개발 목적

- `main.py`에 남은 settings/dashboard/audit/jobs 성격의 admin route를 별도 module로 분리한다.
- 분리 route는 `app.state.repos.*` facade 사용을 유지한다.
- MCP handler helper 분리, repository.py 본체 분해, Plugin built-in 구현은 다음 Phase로 남긴다.

## 개발 범위

- `coremcp.api.admin_meta` 신설.
- `/v1/me`, `/v1/settings`, `/v1/settings/admin-token/rotate`, `/v1/dashboard/summary` 이동.
- `/v1/tool-invocations`, `/v1/audit-logs`, `/v1/jobs/{job_id}` 이동.
- route export, structural smoke, 문서 갱신.

## 제외 범위

- MCP handler helper 분리.
- repository.py 본체 도메인 분해.
- Plugin Framework built-in/외부 loading.
- SaaS/team/workspace/marketplace/billing 기능.

## 참조 문서

- [AGENTS.md](../AGENTS.md)
- [dev-plan/implement_20260515_220722.md](implement_20260515_220722.md)
- [coremcp-docs/10-test-plan.md](../coremcp-docs/10-test-plan.md)

## 공통 진행 규칙

- route 분리는 behavior-preserving refactor로 유지한다.
- 각 Phase는 자체 테스트 완료 후에만 다음 Phase로 이동한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약

- [x] Phase 1 완료 — admin_meta router 신설
- [x] Phase 2 완료 — main.py inline route 제거 및 registration 연결
- [x] Phase 3 완료 — 검증/문서 갱신

## Phase 1. admin_meta router 신설

### 목표

- settings/dashboard/audit/jobs route를 등록하는 독립 module을 만든다.

### 구현 태스크

- [x] `apps/api/coremcp/api/admin_meta.py` 신설.
- [x] settings/dashboard route 이동.
- [x] admin token rotate route 이동.
- [x] audit/jobs route 이동.

### 자체 테스트

- [x] `cd apps/api && uv run python -m py_compile coremcp/main.py coremcp/api/*.py`

### 이슈 및 수정

- [x] 발견 이슈 없음.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료

## Phase 2. main.py registration 연결

### 목표

- `main.py`에서 inline route block을 제거하고 `register_admin_meta_routes`를 호출한다.

### 구현 태스크

- [x] `coremcp.api` export 갱신.
- [x] `main.py` import/registration 추가.
- [x] 기존 inline route 제거.
- [x] 불필요 import 제거.

### 자체 테스트

- [x] `cd apps/api && uv run pytest tests/test_structure_facades.py tests/test_mcp_gateway.py -q`

### 이슈 및 수정

- [x] 발견 이슈 없음.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료

## Phase 3. 검증/문서 갱신

### 목표

- 전체 smoke와 문서 상태를 최신화한다.

### 구현 태스크

- [x] `README.md`, `TESTING.md`, `coremcp-docs/10-test-plan.md` 갱신.
- [x] structural facade test export 갱신.

### 자체 테스트

- [x] `cd apps/api && uv run pytest -q`
- [x] `make test`
- [x] `git diff --check`

### 이슈 및 수정

- [x] 발견 이슈 없음.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료

## 최종 결과 요약

- `admin_meta` route module로 settings/dashboard/audit/jobs route를 분리했다.
- `main.py`는 약 2,843 LOC 수준으로 감소했다.
- 분리된 route는 `app.state.repos.audit`, `app.state.repos.jobs`, `app.state.repos.repository` facade boundary를 사용한다.
- API pytest 기준은 `151 passed`로 유지됐다.

## 잔여 리스크 / 후속 과제

- MCP handler helper 분리가 다음 안정화 후보로 남아 있다.
- repository.py 본체 분해는 call-site migration 안정화 이후 별도 Phase로 진행한다.
- Plugin built-in/외부 loading은 보안 ADR 선행 전까지 보류한다.
