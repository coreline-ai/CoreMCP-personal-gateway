# implement_20260515_213000.md

작성 일시: `2026-05-15 21:30:00 KST`

이 문서는 남은 운영 검증/구조 refactor 항목 중 현재 로컬에서 안전하게 진행 가능한 작업을 고정한다.

## 개발 목적
- 실제 reboot/Tailscale/OAuth/mobile/long-soak은 운영 환경 의존 검증으로 분리하고, 로컬에서 실행 가능한 helper smoke를 재확인한다.
- FastAPI router 분리를 빅뱅이 아닌 작은 route group부터 시작한다.
- repository call-site migration은 router 분리 이후가 원칙이지만, `RepositoryFacades`를 app state에 연결해 점진 migration entrypoint를 마련한다.

## 개발 범위
- `api/meta.py` 신설: `/health`, `/live`, `/ready`, `/metrics` route group 분리.
- `create_app`에서 `register_meta_routes()` 호출.
- `app.state.repos = RepositoryFacades(repository)` 추가.
- helper smoke: `make external-env-validate`, `make mobile-qa-checklist`, short soak smoke.

## 제외 범위
- 실제 macOS reboot 실행.
- 실제 Tailscale login/Serve/ACL 변경.
- real external OAuth client 등록.
- 물리 모바일 브라우저 직접 QA.
- 1시간 long soak의 대기 완료. 장시간 soak는 운영 host에서 실행한다.
- 전체 route/router 및 repository call-site 빅뱅 migration.

## 참조 문서
- [AGENTS.md](../AGENTS.md)
- [dev-plan/implement_20260515_182234.md](./implement_20260515_182234.md)
- [dev-plan/implement_20260515_211733.md](./implement_20260515_211733.md)
- [TESTING.md](../TESTING.md)

## 공통 진행 규칙
- 현재 STDIO command allowlist 변경을 되돌리지 않는다.
- route path/method/status/response shape를 유지한다.
- token boundary와 bearer per-request invariant를 유지한다.

## Phase 상태 요약
- [x] Phase 1 완료 — Local helper smoke
- [x] Phase 2 완료 — Meta router extraction
- [x] Phase 3 완료 — RepositoryFacades app state entrypoint
- [x] Phase 4 완료 — Tests/docs update

## Phase 1. Local helper smoke
### 목표
- 실제 외부환경 대신 현재 host에서 가능한 운영 helper를 실행한다.

### 구현 태스크
- [x] `make external-env-validate`
- [x] `make mobile-qa-checklist`
- [x] `COREMCP_SOAK_DURATION_SECONDS=60 COREMCP_SOAK_INTERVAL_SECONDS=15 make soak-check`

### 자체 테스트
- [x] 각 명령 exit code 0 확인.

## Phase 2. Meta router extraction
### 목표
- `main.py`의 health/readiness/metrics route를 `coremcp.api.meta`로 분리한다.

### 구현 태스크
- [x] `apps/api/coremcp/api/__init__.py` 추가.
- [x] `apps/api/coremcp/api/meta.py` 추가.
- [x] `main.py` route definitions 제거 및 `register_meta_routes()` 호출.

### 자체 테스트
- [x] `cd apps/api && uv run pytest tests/test_structure_facades.py -q`
- [x] `cd apps/api && uv run pytest tests/test_mcp_gateway.py -q`

## Phase 3. RepositoryFacades app state entrypoint
### 목표
- 전체 call-site migration 전 단계로 `app.state.repos`를 제공한다.

### 구현 태스크
- [x] lifespan에서 `RepositoryFacades(app.state.repository)` 설정.
- [x] 구조 테스트로 `app.state.repos` bundle 존재 확인.

### 자체 테스트
- [x] `cd apps/api && uv run pytest tests/test_structure_facades.py -q`

## Phase 4. Tests/docs update
### 목표
- 변경 후 전체 테스트와 문서 snapshot을 동기화한다.

### 구현 태스크
- [x] `TESTING.md` snapshot 반영.
- [x] dev-plan 체크박스 업데이트.

### 자체 테스트
- [x] `git diff --check`
- [x] `cd apps/api && uv run pytest -q`
- [x] `make test`

## 최종 결과 요약
- Local helper smoke를 실행했다: `make external-env-validate` PASS, 60초 soak 4 checks/0 failures PASS, `make mobile-qa-checklist` 출력 PASS.
- `coremcp.api.meta`를 추가해 `/health`, `/live`, `/ready`, `/metrics` route group을 `main.py`에서 분리했다.
- `app.state.repos = RepositoryFacades(app.state.repository)`를 추가해 repository call-site migration entrypoint를 마련했다.
- 검증: `tests/test_structure_facades.py` 5 passed, `tests/test_mcp_gateway.py` 83 passed, API full pytest 148 passed.

## 잔여 리스크 / 후속 과제
- `make test` 최종 확인까지 완료했다.
- 실제 1시간/8시간 long soak, actual reboot, Tailscale login/Serve/ACL, real OAuth client, 물리 모바일 QA는 운영 host에서 수행해야 한다.
- 다음 구조 refactor는 OAuth router 또는 MCP endpoint router 분리, 이후 repository call-site의 domain facade 이전 순서가 안전하다.
