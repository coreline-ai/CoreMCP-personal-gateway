# implement_20260513_212322.md

작성 일시: `2026-05-13 21:23:22 KST`

이 문서는 “적용된 디자인이 안 나온다”는 실행 화면 이슈를 재검증하고, CoreMCP Web Admin을 실제 Multica 디자인 원본 기준으로 다시 맞추기 위한 수정 작업 계획이다.

## 원인

- 이전 반영은 `cm-*` primitive는 로드됐지만, `/Users/hwanchoi/projects/multica/multica-main`의 실제 디자인 토큰/규칙을 흡수하지 못했다.
- 실행 화면은 여전히 blue gradient, 큰 hero title, 큰 metric 숫자, shadow/card 중심의 SaaS 스타일이었다.
- Multica 원칙은 “neutral surface, color as signal, text-base/text-sm/text-xs, font-medium, no decorative shadow/gradient”다.

## 개발 범위

- `apps/web/app/globals.css`를 Multica OKLCh token 기반으로 재정의한다.
- Tailwind theme token을 CSS variable 기반으로 정리한다.
- Web Admin shell/sections/detail 화면의 큰 타이포·semibold·shadow 중심 스타일을 제거한다.
- `docs/design` 문서를 Multica 원본 참조와 코드 기준으로 갱신한다.
- 실행 중인 Web Admin을 재시작하고 route smoke/browser screenshot으로 확인한다.

## 제외 범위

- Multica의 team/workspace/marketplace/billing 기능 도입.
- 외부 UI library/shadcn package 추가.
- 외부 LLM API dependency 추가.
- OAuth/CIMD 기능 변경.

## Phase 상태 요약

- [x] Phase 1. 실행 화면과 CSS 적용 상태 재확인
- [x] Phase 2. Multica 디자인 원본 분석
- [x] Phase 3. CoreMCP Web 디자인 primitive 재적용
- [x] Phase 4. 실행 검증 및 문서 최종 확인

## Phase 1. 실행 화면과 CSS 적용 상태 재확인

- [x] Playwright snapshot으로 `/clients` 실행 화면 구조 확인.
- [x] `cm-*` class 자체는 로드되는 것을 확인.
- [x] 문제는 CSS 미로딩이 아니라 디자인 원본 불일치임을 확인.

## Phase 2. Multica 디자인 원본 분석

- [x] `/Users/hwanchoi/projects/multica/multica-main/docs/design.md` 확인.
- [x] `packages/ui/styles/tokens.css`의 OKLCh token 확인.
- [x] `packages/ui/styles/base.css`, `card.tsx`, `button.tsx`, `page-header.tsx` 패턴 확인.

## Phase 3. CoreMCP Web 디자인 primitive 재적용

- [x] `globals.css` body gradient 제거, neutral background 적용.
- [x] `cm-card`, `cm-shell-card`, `cm-panel`, `cm-button`, `cm-input`을 Multica식 neutral/minimal surface로 변경.
- [x] `cm-page-title`, `cm-section-title`, metric typography를 `text-base/font-medium` 중심으로 축소.
- [x] shell navigation/tabs를 rounded pill + border style에서 subtle muted active style로 변경.
- [x] visible sections와 service detail에서 `font-semibold`, 큰 text size, shadow 중심 스타일 제거.
- [x] `ToolIcon` shadow/card 강조 제거.

## Phase 4. 실행 검증 및 문서 최종 확인

- [x] `pnpm lint`
- [x] `pnpm build`
- [x] `infra/scripts/coremcp-launchctl.sh restart`
- [x] `infra/scripts/web-route-smoke.sh`
- [x] Playwright screenshot으로 실제 화면 확인
- [x] `git diff --check`
