# implement_20260523_195307.md

작성 일시: `2026-05-23 19:53:07 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

직전 4 cycle 의 잔여 후속 6 항목을 한 dev-plan 으로 마무리. 신기능 없음, 안정화 · 계약 안정성 · 확장성 · 운영 가이드 정리.

## 개발 범위 (이 6 Phase)

- Phase 1: backend response_model 추가 (Dashboard / Settings / Audit / Invocation 4 라우트)
- Phase 2: ToolOverride / ToolPreset 의 backend `Literal[...]` 타이트닝 + frontend alias 완전 전환
- Phase 3: ADR-046 Step 2 — `AuditRepositoryMixin` TYPE_CHECKING 전환
- Phase 4: ADR-046 Step 3 — 나머지 5 mixin 일괄 TYPE_CHECKING
- Phase 5: ADR-047 strict 선행 작업 (TypedDict / AppContext / lib stub)
- Phase 6: 운영 Redis 환경 연결 가이드

## 제외 범위

- 실제 운영 Tailscale ACL 설정
- pyright `strict` 전면 도입
- 새 기능, SaaS multi-tenant, plugin marketplace
- 외부 LLM API 의존성 추가

## 참조 문서

- 이전 dev-plan: [`dev-plan/implement_20260523_094019.md`](implement_20260523_094019.md)
- [`coremcp-docs/13-adr.md`](../coremcp-docs/13-adr.md) — ADR-042~047

## 공통 진행 규칙

- 각 Phase 자체 테스트 완료 후에만 다음 Phase 진행.
- 동작 보존 우선 — 시그니처 / 응답 shape / 로그 변경 금지.
- 모든 Phase 완료 후 `make test` + `pnpm typecheck && pnpm build && pnpm lint` + `uv run pyright` 0 errors 통과 후 commit.

## Phase 상태 요약

- [x] Phase 1 완료 — backend response_model 4 라우트
- [x] Phase 2 완료 — Literal 타이트닝 + frontend alias 완전 전환
- [x] Phase 3 완료 — Audit mixin TYPE_CHECKING (+ 7 None guards)
- [x] Phase 4 완료 — 5 mixin 일괄 TYPE_CHECKING (Services/Catalog/Toolbox/Credentials/Connections)
- [x] Phase 5 완료 — strict 카운트 943→387 (ADR-046 효과), ADR-047 갱신
- [x] Phase 6 완료 — Redis 운영 가이드 §5.7

---

## Phase 1. backend response_model 4 라우트

- [x] `_schemas.py` 에 `SettingsResponse`, `DashboardSummary`, `ToolInvocationSummary/List`, `AuditLogSummary/List` 추가 (extra="allow")
- [x] `/v1/settings`, `/v1/dashboard/summary`, `/v1/tool-invocations`, `/v1/audit-logs` 에 response_model 부여
- [x] 회귀: 219 tests, pyright 0 errors

## Phase 2. Literal 타이트닝 + frontend 완전 alias

- [x] `ToolOverrideSummary.permission_level: Literal["hidden", "visible_only", "callable"]`
- [x] `ToolPresetResponse.preset: Literal["readonly", "full_access", "dangerous_off"]`
- [x] API 재기동 + `pnpm gen:api` 재실행 (api.generated.ts 갱신)
- [x] `apps/web/lib/api.ts` 의 `ToolOverrideSummary`, `ToolPresetResponse` 를 generated alias 로 완전 전환
- [x] 회귀: 219 tests, pyright 0, web typecheck/build/lint 통과

## Phase 3. Audit mixin TYPE_CHECKING (ADR-046 Step 2)

- [x] `repository_audit.py` 의 `# pyright: ...` 디렉티브 제거
- [x] `if TYPE_CHECKING:` block 에 host attribute (`db` property, `dumps_json`, `loads_json`, `_row_to_dict`) 선언
- [x] COUNT/aggregate SELECT 의 `fetchone()` 결과 7곳 `assert row is not None` 추가 (동작 변경 0)
- [x] ADR-046 Step 2 완료 기록
- [x] 회귀: 219 tests, pyright 0

## Phase 4. 5 mixin 일괄 TYPE_CHECKING (ADR-046 Step 3)

- [x] `Services / Catalog / Toolbox / Credentials / Connections` 5 mixin 일괄
- [x] 각 mixin 의 `if TYPE_CHECKING:` block 에 host attribute + cross-mixin 메서드 (`log_audit`, `get_mcp_service`, `get_external_connection`) stub 선언
- [x] `log_audit` 반환 타입 `-> str` (audit_id) 로 정확히 선언
- [x] `repository_catalog.py:472` 의 `fetchall()` 결과 `list()` wrap (Iterable→Sequence)
- [x] 모든 mixin 디렉티브 제거 완료
- [x] 회귀: 219 tests, pyright 0

---

## Phase 5. ADR-047 strict 선행 작업 (surgical)

### 목표

ADR-047 의 punch list 3 항목 중 본 cycle 한도 안에서 surgical 진행. strict 활성화는 다음 cycle.

### 구현 태스크

- [x] strict dry-run 측정 — 943 → **387 errors** (-59%, Phase 3-4 의 mixin TYPE_CHECKING 효과)
- [x] 카테고리 분포: `reportUnknownMemberType` 116, `reportUnknownVariableType` 112, `reportUnknownArgumentType` 86, `reportUnusedFunction` 54, 기타 19
- [x] ADR-047 갱신 — 2차 측정 카운트 + 잔여 punch list 기록
- [ ] TypedDict / lib stub surgical fix — 387건 중 본 cycle 한도 안에서 큰 임팩트는 없음. 다음 cycle 에서 dict.get() 핫스팟별 TypedDict 도입으로 단계적 처리.

### 자체 테스트

- [x] standard mode 0 errors 유지 (Phase 4 결과)
- [x] `uv run pytest -q` 219 tests 통과

---

## Phase 6. 운영 Redis 환경 연결 가이드

### 목표

`coremcp-docs/12-operations.md` 에 Redis 백엔드 운영 가이드 단락 추가.

### 구현 태스크

- [x] `coremcp-docs/12-operations.md` 에 **§5.7 Redis rate limiter 운영** 단락 추가
  - **5.7.1 환경 변수** — `COREMCP_RATE_LIMIT_BACKEND` / `COREMCP_RATE_LIMIT_REDIS_URL`
  - **5.7.2 Tailscale 위 Redis 공유** — 개인 사용 가이드 (`tailscale serve --tcp`)
  - **5.7.3 연결 검증** — `redis-cli ping`, 로그 grep
  - **5.7.4 장애 대응** — InMemory fallback 자동 회복 + audit_logs 비교 가이드

### 자체 테스트

- [x] markdown 구조 정상 (§5.7 4 subsection)
- [x] `uv run pytest -q` 219 tests 통과
- [x] `uv run pyright` 0 errors
