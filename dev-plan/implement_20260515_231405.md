# implement_20260515_231405.md

작성 일시: `2026-05-15 23:14:05 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
- `main.py`에 남아 있는 RPC helper 본체를 `coremcp.mcp` 모듈로 분리한다.
- `/mcp` resources/prompts/tools handler 분리 흐름을 이어가되, 동작 변경 없이 결합도만 낮춘다.

## 개발 범위
- `_request_service_rpc`, `_request_default_downstream_rpc` 본체를 신규 `mcp/rpc.py`로 이동.
- app-state 의존성은 명시적 dependency dataclass로 감싼다.
- 기존 handler deps와 call-site는 wrapper를 통해 호환 유지한다.
- 구조 export/test를 갱신한다.

## 제외 범위
- `_handle_tools_call` 본체 분리는 다음 phase로 유지한다.
- repository.py 도메인 분해는 이번 범위에서 제외한다.
- RPC 동작, circuit breaker 정책, notification fan-in 정책, stdio lifecycle 정책은 변경하지 않는다.
- Plugin Framework, built-in plugin, access control 확장은 하지 않는다.

## 참조 문서
- [AGENTS.md](../AGENTS.md)
- [coremcp-docs/10-test-plan.md](../coremcp-docs/10-test-plan.md)
- [dev-plan/implement_20260515_225153.md](./implement_20260515_225153.md)
- [dev-plan/implement_20260515_225815.md](./implement_20260515_225815.md)

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약
- [x] Phase 1 완료
- [x] Phase 2 완료
- [x] Phase 3 완료

## Phase 1. RPC helper 의존성 경계 정의
### 목표
- main.py의 RPC helper가 실제로 필요로 하는 의존성을 명시한다.

### 구현 태스크
- [x] `_request_service_rpc` 의존성 목록 확인.
- [x] `_request_default_downstream_rpc` 의존성 목록 확인.
- [x] 신규 `RpcHelperDeps` dataclass 설계.

### 자체 테스트
- [x] 관련 grep/정적 확인.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 분리 대상과 wrapper 유지 방식 확정.
- [x] 다음 Phase 진행 가능.

## Phase 2. `mcp/rpc.py` 분리
### 목표
- RPC helper 본체를 main.py 밖으로 이동하고 기존 동작을 유지한다.

### 구현 태스크
- [x] `apps/api/coremcp/mcp/rpc.py` 추가.
- [x] `mcp/__init__.py` export 갱신.
- [x] `main.py` wrapper가 신규 helper를 호출하도록 변경.
- [x] create_app/lifespan state 초기화 흐름에 무리 없는지 확인.

### 자체 테스트
- [x] 구조 export 테스트 갱신.
- [x] RPC helper 관련 단위/통합 테스트 통과.

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료.
- [x] 자체 테스트 완료.
- [x] 다음 Phase 진행 가능.

## Phase 3. 검증/문서 마무리
### 목표
- 현재 테스트 기준과 문서를 맞춘다.

### 구현 태스크
- [x] `README.md` 안정화 문장 갱신.
- [x] `coremcp-docs/10-test-plan.md` 안정화 문장 갱신.
- [x] 본 dev-plan 체크박스 완료 처리.

### 자체 테스트
- [x] `cd apps/api && uv run pytest -q`
- [x] `make test`
- [x] `git diff --check`

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료.
- [x] 자체 테스트 완료.
- [x] 병합 가능 상태.
